# 自定义改动清单

本文档记录本仓库**对上游的修改**与**新建的自定义文件**，用于合并上游时减少冲突。

**当前分支**：`v.1.17.6多用户`（基于上游 `v0.1.1` / commit `a7c8f38a5`）

> **维护规则**
> - 每次提交自定义改动后，同步更新本文档
> - 合并上游前必做：打开本文档 → 重点盯"改过的上游文件"列表 → 用 `git diff upstream-tag..HEAD -- <file>` 逐个检查冲突
> - 上游 tag：`v0.1` 系列；本分支从 `v0.1.1` 切出，目标上游 tag：`v0.1.196` 及之后

---

## 1. 改过的上游文件（高风险点）

### 1.1 Schema.Defect 调用化（v0.1.6 era effect 兼容）

**背景**：v0.1.1 时代 effect 4.0.0-beta.74 把 `Schema.Defect` 从常量改成了构造函数，但
v0.1.1 era 代码仍用常量用法。启动 `opencode serve` 时模块加载期就抛
`TypeError: undefined is not an object (evaluating 'ast.encoding')`。

**修法**：把所有 `Schema.Defect` 改成 `Schema.Defect()`，与上游 commit
`4f1ae1604 chore: upgrade Effect to beta 83 (#32340)` 在 dev 分支上做的事一字不差。

**影响范围**：22 个上游文件，每个只动 1 行（加一对括号）。涉及 5 个 package。

| 文件 | 行 |
| --- | --- |
| `packages/effect-drizzle-sqlite/examples/basic.ts` | 28 |
| `packages/llm/src/schema/errors.ts` | 205 |
| `packages/llm/src/schema/events.ts` | 178 |
| `packages/core/src/account.ts` | 38, 43, 50, 97 |
| `packages/core/src/aisdk.ts` | 112 |
| `packages/core/src/control-plane/move-session.ts` | 51 |
| `packages/core/src/fs-util.ts` | 16 |
| `packages/core/src/git.ts` | 35, 42 |
| `packages/core/src/integration.ts` | 152 |
| `packages/core/src/npm.ts` | 18 |
| `packages/core/src/process.ts` | 12 |
| `packages/core/src/ripgrep.ts` | 45 |
| `packages/core/src/tool-output-store.ts` | 31 |
| `packages/core/src/util/effect-flock.ts` | 27 |
| `packages/opencode/src/account/schema.ts` | 36, 41, 48, 95 |
| `packages/opencode/src/auth/index.ts` | 40 |
| `packages/opencode/src/control-plane/workspace.ts` | 139 |
| `packages/opencode/src/lsp/client.ts` | 31 |
| `packages/opencode/src/provider/provider.ts` | 1079, 1088 |
| `packages/stats/core/src/athena.ts` | 20 |
| `packages/stats/core/src/database.ts` | 48, 55 |
| `packages/stats/server/src/ingest.ts` | 18 |

**commits**：
- `84a913f59` `fix(effect-drizzle-sqlite): call Schema.Defect() in v0.1.6 era`
- `76df8ebb2` `fix(llm): call Schema.Defect() in v0.1.6 era`
- `3fdf4f2b8` `fix(core): call Schema.Defect() in v0.1.6 era`
- `35b9e796f` `fix(stats): call Schema.Defect() in v0.1.6 era`
- `d85b54c06` `fix(opencode): call Schema.Defect() in v0.1.6 era`

**业务原因**：让 v0.1.1 era 代码能跑在 effect 4.0.0-beta.74 上。

**合并上游时**：上游 commit `4f1ae1604` 已经做过完全相同的修改，**自动合并无冲突**。
如果上游 v0.1.196+ 改动了 effect 版本到 beta 83+，这些修改已经是上游默认行为，**应
整体丢弃**本节列出的 22 个文件改动。

### 1.2 技能面板数据刷新（`skills.json`）

**背景**：v0.1.1 era 技能面板有 5 个 `developing: true` 占位技能（生成PPT、文生图、图生文、
视频制作、视频总结），现在转正为可用技能，绑定了对应 skillId 并补齐 tooltip/keyword/case/prompt。
同时简化了若干 prompt 模板的保存路径占位。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/app/public/yejian/skills.json` | +68 / -39 |

**commits**：
- `3ad3ff21e` `chore(yejian): refresh skills.json panel data`

**具体改动**：
- 5 个 `developing: true` 技能转正：
  - `生成PPT` → `PPT 专业制作`（skillId `ppt-master`）
  - `文生图` → `文本生成图片`（skillId `agnes-ai-generation`）
  - `图生文` → `图片编辑`（skillId `agnes-ai-generation`）
  - `视频制作` → `视频生成`（skillId `agnes-ai-generation`）
  - `视频总结` → 补全字段（skillId `agnes-ai-generation`）
- Prompt 模板微调：`【XXXX文件夹】` → `用户文件夹下`，prompt 字符串结尾加 `\n\n` 便于换行输入

**业务原因**：让 5 个新技能在 v0.1.6 era 仓库下被技能面板识别并可用。

**合并上游时**：上游 v0.1.196+ 一定会持续迭代 skills.json，**逐行 conflict 概率高**。
合并前先 `git diff upstream-tag..HEAD -- packages/app/public/yejian/skills.json` 导出补丁，
合并完再重放；如果上游重写了文件结构，需要重做。

### 1.3 前端 dev proxy 后端端口改回默认 4096

**背景**：v.1.17.6 多用户分支之前的人把 `packages/app/vite.config.ts:29` 的
`const backend = "http://127.0.0.1:4098"` 改成了 4098，导致前端 dev 必须配合后端
`--port 4098` 才能工作。dev 默认是 4096。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/app/vite.config.ts` | L29: `4098` → `4096` |

**commits**：待提交（计划 `fix(yejian): align vite dev proxy backend port to 4096 default`）

**业务原因**：跟 dev 默认行为对齐，启动后端可省 `--port 4098`。

**合并上游时**：dev 上 `vite.config.ts:29` 是 4096，**自动合并无冲突**。

### 1.4 Docker 打包前适配：scryptSync 密码哈希 + bootstrapUsers 多用户预置

**背景**：v.1.17.6 多用户分支准备打包成 Docker 镜像前发现两个必修问题：
1. `packages/core/src/user/index.ts` 用 `@node-rs/argon2`，在 Linux 容器里因 native binding
   路径硬编码 Windows 路径会崩（`TypeError: must be absolute path`）
2. `bootstrapAdmin()` 只能从 `OPENCODE_SERVER_USERNAME/PASSWORD` 单条创建 admin，
   没有"一组用户"清单预置入口

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/core/src/user/index.ts` | import 替换 + 新增 `hashPassword/verifyPasswordHash` 辅助函数 + 4 处使用点替换（行 105/142/194/209）+ `hashPassword` 改为 `export`（供迁移脚本复用） |
| `packages/core/src/database/migration/20260625120001_seed_initial_users.ts` | `import { hash } from "@node-rs/argon2"` → `import { hashPassword } from "../../user"`，调用点同步改（避免迁移脚本跨平台崩溃 + hash 格式统一） |
| `packages/core/package.json` | 删除 `"@node-rs/argon2": "2.0.2"` 依赖 |
| `packages/opencode/src/cli/cmd/serve.ts` | 新增 `bootstrapUsers()` 函数（行 37-84）+ handler 里调用（行 107-111）+ `bootstrapAdmin()` 无 `OPENCODE_SERVER_PASSWORD` 时改为静默跳过（不再 `process.exit(1)`） |

**commits**：待提交（计划 `fix(yejian): use scryptSync for cross-platform password hashing` + `feat(yejian): add bootstrapUsers() for pre-seeding users` + `fix(yejian): bootstrapAdmin skip silently when no password`）

**业务原因**：
1. 跨平台密码哈希用 `node:crypto.scryptSync`（OWASP 2024 推荐参数 N=2^14, r=8, p=1），
   避免 Linux 容器启动崩溃
2. 通过 `OPENCODE_SEED_USERS_FILE` 环境变量从 JSON 文件预置多用户，容器内路径默认
   `/etc/opencode/seed-users.json`

**使用方式**（容器启动）：
```powershell
docker run -d --name yejian-AIworkbench -p 8088:8088 `
  -v "E:\AI\YEJIAN:/YEJIAN" `
  -v "D:\AI\AIworkbench-data/root:/root" `
  -v "D:\AI\YEJIAN\config\seed-users.json:/etc/opencode/seed-users.json:ro" `
  -e OPENCODE_SEED_USERS_FILE=/etc/opencode/seed-users.json `
  --env-file "E:\AI\dockerimage\api-keys.env" `
  --restart always yejian-opencode:v0.0.6
```

**合并上游时**：
1. dev 上 `user/index.ts` 也用 `@node-rs/argon2`（项目记忆里那条踩坑正是从这里的实际经验），
   上游没修，**冲突极少**
2. `serve.ts` 的 `bootstrapAdmin()` 是 v.1.17.6 自定义加的，dev 上没有，**整体重放**
3. `bootstrapUsers()` 是全新功能，**dev 上无冲突**
4. `migration/20260625120001_seed_initial_users.ts` 是 v.1.17.6 自定义加的，dev 上没有，**整体重放**

### 1.5 Docker v0.0.6 打包：多用户预置 + ppt-master 依赖

**背景**：v0.0.6 镜像需满足：(1) api-keys.env 无 `OPENCODE_SERVER_USERNAME/PASSWORD`（用户不希望输两次密码）；
(2) ppt-master skill 依赖装进镜像；(3) 启动默认打开 `/YEJIAN` 项目；(4) seed-users.json 烤入镜像（用户
`docker run` 命令未挂载种子文件）。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `docker/seed-users.json` | **新建**，admin 账号 yejian/Yejian2016（首次登录强制改密） |
| `docker/requirements-ppt.txt` | **新建**，ppt-master 的 17 个 Python 依赖（svglib 方案 B 精简版，不装 cairosvg/pandoc） |
| `docker/Dockerfile` | 顶部注释加 v0.0.6 changes；新增 `COPY + uv pip install requirements-ppt.txt`；新增 `COPY seed-users.json /etc/opencode/seed-users.json` + `ENV OPENCODE_SEED_USERS_FILE=/etc/opencode/seed-users.json` |
| `docker/build.ps1` | 默认 tag `v0.0.3` → `v0.0.6`；workbenchDir `D:\AI\AIworkbench` → `E:\AI\YEJIAN`；dataDir `D:\AI\AIworkbench-data` → `E:\AI\AIworkbench-data`；挂载 `/workspace` → `/YEJIAN`；工作目录 `/workspace` → `/YEJIAN`；加 `--restart always` |
| `docker/readme.md` | 顶部镜像名称 v0.0.3 → v0.0.6；变更记录加 v0.0.6 行；末尾加 v0.0.6 附录（需求背景 / 修改文件 / 关键设计决策 / 启动命令 / 验证步骤 / 坑 15） |

**commits**：待提交（计划按依赖顺序拆分：代码层 → docker 层 → 文档层）

**业务原因**：
1. `seed-users.json` 烤入镜像，用户 `docker run` 命令无需挂载种子文件
2. `bootstrapAdmin()` 无密码时静默跳过（1.4 节），`bootstrapUsers()` 从烤入的种子文件预置 admin
3. `auth.ts` 的 `required()` 在无 `OPENCODE_SERVER_PASSWORD` 时返回 false，HTTP Basic Auth 自动禁用，
   多用户登录系统（cookie token）接管认证
4. `layout.tsx` L559 已有 `openProject(last ?? "/YEJIAN", true)` 逻辑，全新容器首次启动自动打开 /YEJIAN
5. ppt-master 依赖用 svglib 方案 B（~5MB）替代 cairosvg（~25MB），不装 pandoc，精简镜像体积

**使用方式**（容器启动）：
```powershell
docker run -d --name yejian-AIworkbench -p 80:8088 `
  -v "E:\AI\YEJIAN:/YEJIAN" `
  -v "E:\AI\AIworkbench-data/root:/root" `
  -v "E:\AI\AIworkbench-data/tmp:/tmp" `
  -w /YEJIAN `
  --env-file "E:\AI\dockerimage\api-keys.env" `
  --restart always `
  --hostname 0.0.0.0 `
  yejian-opencode:v0.0.6
```

**合并上游时**：
1. `docker/` 目录是 v.1.17.6 自定义加的，dev 上没有，**整体重放**
2. `seed-users.json` / `requirements-ppt.txt` 是全新文件，**dev 上无冲突**
3. `Dockerfile` / `build.ps1` / `readme.md` 是 v.1.17.6 自定义加的，**整体重放**

### 1.6 Docker v0.0.8 binary 编译：xd.node undefined 修复

**背景**：v0.0.8 镜像用多阶段构建在 Linux 容器内编译 binary。容器启动报
`TypeError: undefined is not an object (evaluating 'xd.node')`，web 服务返回 404。
根因是合并提交 `859f44c3e` 删除了自引用命名空间导出 `export * as Database from "./database"`，
改成了 `export const Database = { Service, defaultLayer, node }`。

Bun compile 把每个模块编译成 lazy 函数，`export const` 导出在模块体函数执行时才赋值。
如果模块 A 引用模块 B 的导出，但模块 B 的 lazy 函数还没调用，模块 B 的 `export const` 导出
就是 undefined。自引用命名空间导出 `export * as Xxx from "..."` 创建的 namespace 对象是
live binding，即使模块体未执行，namespace 对象本身也存在。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/core/src/database/database.ts` | L66 `export const Database = { Service, defaultLayer, node }` → `export * as Database from "./database"` |
| `packages/core/src/user/index.ts` | L263 `export const User = { Service, defaultLayer, node }` → `export * as User from "./index"` |
| `packages/core/src/auth-token/index.ts` | L135 `export const AuthToken = { Service, defaultLayer, node }` → `export * as AuthToken from "./index"` |

**commits**：待提交（计划 `fix(yejian): restore self-referencing namespace exports for Bun compile`）

**业务原因**：让 Bun compile 后的 binary 能正常运行，不报 `xd.node` undefined 错误。

**合并上游时**：
1. `database.ts` 的 `export * as Database from "./database"` 是 v0.0.3 原有模式，dev 上也是这个模式，**自动合并无冲突**
2. `user/index.ts` 和 `auth-token/index.ts` 是 v.1.17.6 自定义加的，dev 上没有，**整体重放**

---

### 1.7 源码直跑模式启动修复：Schema.Defect 回退 + 循环依赖打破（2026-07-17）

**背景**：从 Docker binary 部署切换回本地源码开发模式（`bun run --conditions=browser ./src/index.ts serve`）后连续遇到 3 个阻塞问题，根因都是"binary 编译模式 vs 源码直跑模式"差异。

**改动 1：Schema.Defect 回退（22 个文件，31 行）**

v0.1.6 时代（1.1 节）为了适配 effect 做的 `Schema.Defect → Schema.Defect()` 调用化，在当前 effect 4.0.0-beta.74 下反而是错的——`Schema.Defect` 在这个版本是常量 schema 对象，不是函数。全部反向回退为 `Schema.Defect`。

| 文件 | 行 |
| --- | --- |
| `packages/llm/src/schema/errors.ts` | 205 |
| `packages/llm/src/schema/events.ts` | 178 |
| `packages/core/src/account.ts` | 38, 43, 50, 97 |
| `packages/core/src/aisdk.ts` | 112 |
| `packages/core/src/control-plane/move-session.ts` | 51 |
| `packages/core/src/fs-util.ts` | 16 |
| `packages/core/src/git.ts` | 35, 42 |
| `packages/core/src/integration.ts` | 152 |
| `packages/core/src/npm.ts` | 18 |
| `packages/core/src/process.ts` | 12 |
| `packages/core/src/ripgrep.ts` | 45 |
| `packages/core/src/tool-output-store.ts` | 31 |
| `packages/core/src/util/effect-flock.ts` | 27 |
| `packages/opencode/src/account/schema.ts` | 36, 41, 48, 95 |
| `packages/opencode/src/auth/index.ts` | 40 |
| `packages/opencode/src/control-plane/workspace.ts` | 139 |
| `packages/opencode/src/lsp/client.ts` | 31 |
| `packages/opencode/src/provider/provider.ts` | 1079, 1088 |
| `packages/stats/core/src/athena.ts` | 20 |
| `packages/stats/core/src/database.ts` | 48, 55 |
| `packages/stats/server/src/ingest.ts` | 18 |

**改动 2：循环依赖打破（3 改 + 1 新建）**

源码直跑报 `Cannot access 'node' before initialization`（TDZ）。循环依赖链：`database.ts → migration.ts → seed_initial_users.ts → user/index.ts（import hashPassword）→ database.ts`。binary 模式 lazy 求值绕过 TDZ，源码模式 ESM 同步求值暴露循环。

| 文件 | 改动 |
| --- | --- |
| `packages/core/src/user/password.ts` | **新建**：从 `user/index.ts` 拆出 scrypt 代码（`hashPassword`/`verifyPasswordHash`/`validatePassword`），叶子模块不 import 任何业务模块 |
| `packages/core/src/user/index.ts` | 删 scrypt 代码 + 改 `import { hashPassword, ... } from "./password"` + `export { hashPassword, validatePassword } from "./password"`（保持外部 API 不变）+ 自引用 `export * as User` 移到文件第 1 行 |
| `packages/core/src/database/database.ts` | 自引用 `export * as Database from "./database"` 移到文件第 1 行 |
| `packages/core/src/auth-token/index.ts` | 自引用 `export * as AuthToken from "./index"` 移到文件第 1 行 |
| `packages/core/src/database/migration/20260625120001_seed_initial_users.ts` | L5 `import { hashPassword } from "../../user"` → `from "../../user/password"` |

**commits**：待提交（计划按依赖顺序拆 6 个 commit）

**业务原因**：让本地源码模式能正常启动，与 Docker binary 模式行为一致。

**合并上游时**：
1. Schema.Defect 回退是**反向回退** v0.1.6 适配（1.1 节），合并上游时需对比上游 effect 版本——若上游已升到 beta 83+ 且做了 `Schema.Defect()` 适配，需保留上游写法
2. `password.ts` 是新建文件，低风险
3. `user/index.ts` / `database.ts` / `auth-token/index.ts` 的自引用移第 1 行，与上游 dev `database.ts` 一致，自动合并无冲突
4. `seed_initial_users.ts` 是自定义文件，整体重放

---

### 1.8 titlebar 增加帮助按钮（2026-07-17）

**背景**：新建会话页面顶栏需要在用户设置图标旁增加帮助按钮，点击在新窗口打开 `yejian_help.pdf`。

| 文件 | 改动 |
| --- | --- |
| `packages/app/src/components/titlebar.tsx` | L31 新增 `import { HelpButton } from "./help-button"`；L712（旧版布局）和 L746（V2 布局）在 `<UserMenu />` 前加 `<HelpButton />` |

**commits**：待提交（计划 `feat(yejian): add help button to titlebar opening yejian_help.pdf`）

**业务原因**：提供帮助文档入口，用户点击问号图标即可在新标签页查看 PDF 帮助手册。

**合并上游时**：
1. `titlebar.tsx` 是上游文件，改动集中在 import 行 + 两个 `<UserMenu />` 前各加一行 `<HelpButton />`，带 `// 自定义：` 注释，冲突易解决
2. 若上游重构了 titlebar 布局，需手动在新的 `<UserMenu />` 位置前重新插入 `<HelpButton />`

---

### 1.9 系统提示词注入当前登录用户实名（2026-07-18）

**背景**：项目已采用实名登录，会话输出文件需放在用户实名文件夹内 / 用实名命名产物。
但用户不愿每次在提示词框手动告诉大模型自己是谁。需要让大模型在每次会话中**自动**知道当前登录用户身份。

**做法**：每次拼装 system prompt 时，根据 `session.user_id` 从用户表查出实名，
追加 `<current_user>` 块到 system prompt 末尾。主对话和所有子代理（plan/explore/general/task）全部继承。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/opencode/src/session/user-context.ts` | **新建**：`formatUserContext(user)` 把 UserInfo 格式化成 `<current_user>` 块字符串 |
| `packages/opencode/src/session/prompt.ts` | L67-69 新增 `import { User }` / `import { formatUserContext }`；L135-136 在 `loop` 内 `yield* User.Service`；L1343-1348 在 `system` 数组末尾 push `<current_user>` 块（带 `// yejian:` 注释）；L1600-1601 `defaultLayer` 的 `Layer.mergeAll` 加 `User.defaultLayer`；L1735-1736 `LayerNode.make` 列表加 `User.node` |
| `packages/opencode/src/effect/app-runtime.ts` | L51-52 新增 `import { User }`；L104-105 `AppLayer` 的 `Layer.mergeAll` 加 `User.defaultLayer` |
| `packages/opencode/src/server/routes/instance/httpapi/server.ts` | `LayerNode.make` 列表加 `User.node`（在 `SessionPrompt.node` 后） |
| `packages/opencode/src/control-plane/workspace.ts` | `defaultLayer` 链加 `Layer.provide(User.defaultLayer)`；`node` 的 `LayerNode.make` 列表加 `User.node`（在 `SessionPrompt.node` 后） |

**commits**：待提交（计划 `feat(yejian): inject current user identity into system prompt`）

**业务原因**：让大模型无需用户输入即可知道用户身份，主动按实名命名输出文件、目录、报告等产物。

**合并上游时**：
1. `user-context.ts` 是新建文件，低风险，**无冲突**
2. `prompt.ts` / `app-runtime.ts` / `httpapi/server.ts` / `control-plane/workspace.ts` 是上游文件，改动集中在 import 行 + `Layer.mergeAll` / `LayerNode.make` 列表里各加一行 `User.defaultLayer` / `User.node` + `loop` 内 4-6 行 user 查询，**带 `// yejian:` 注释**，冲突易解决
3. 若上游重构了 Layer 组合方式（如把 `Layer.mergeAll` 换成别的 API），需手动把 `User.defaultLayer` / `User.node` 加到新的组合点
4. **关键约束**：`SessionPrompt.defaultLayer` 内部的 `Layer.mergeAll` 必须加 `User.defaultLayer`，否则 `loop` 内 `yield* User.Service` 会报 `Service not found: @opencode/UserService`

**验证方式**：
1. 启动后端：`cd packages/opencode && $env:OPENCODE_SEED_USERS_FILE="d:\AI\opencode\docker\seed-users.json" && bun run --conditions=browser ./src/index.ts serve --port 4096`
2. 启动前端：`cd packages/app && bun dev`
3. 浏览器登录后开新会话，发任意消息，在 Context 标签页查看 system prompt 末尾是否出现 `<current_user>` 块

---

### 1.10 新建会话页面删除左下角帮助按钮（2026-07-19）

**背景**：1.8 节在 titlebar 顶栏右上角加了帮助按钮（保留），但后续在 `layout.tsx` 的两处 `<main>` 后又各加了一个 `<HelpButton />`，渲染在左下角，与顶栏右上角重复。用户要求删除左下角两处。

| 文件 | 改动 |
| --- | --- |
| `packages/app/src/pages/layout.tsx` | 删除 L66 `import { HelpButton }`；删除 L2367（V2 布局 `<main>` 后）和 L2521（旧布局 `<main>` 后）两处 `<HelpButton />` |

**业务原因**：消除重复帮助入口，保留顶栏右上角问号图标即可。

**合并上游时**：`layout.tsx` 是上游文件，本次只**删除**自定义加的两行 JSX + 一行 import（无新增逻辑），冲突极小；若上游重构了 `<main>` 区域，按"删除 HelpButton 引用"原则处理即可。

---

### 1.11 新建会话页面补充 Ctrl+\ 切换文件树快捷键（2026-07-19）

**背景**：新建会话页面（`/new-session` draft 页面）未使用 `useSessionCommands`，所以没有注册 `fileTree.toggle` 命令，导致 `Ctrl+\` 快捷键不能关闭文件树。普通会话页面通过 `use-session-commands.tsx` L466-472 注册了此命令。

| 文件 | 改动 |
| --- | --- |
| `packages/app/src/pages/new-session.tsx` | L8/L11 新增 `import { useCommand }` / `import { useLanguage }`；L33-34 函数体内新增 `const command = useCommand()` / `const language = useLanguage()`；L68-80 调用 `command.register("new-session", ...)` 注册 `fileTree.toggle` 命令（id/title/category/keybind/onSelect 完全对齐 `use-session-commands.tsx` 的同名命令，`shown()` 条件换成 `settings.visibility.fileTree()`） |

**业务原因**：补齐新建会话页面的 `Ctrl+\` 快捷键，与普通会话页面行为一致。

**合并上游时**：
1. `new-session.tsx` 是上游文件，本次改动集中在 import 行 + 函数体内一段独立的 `command.register` 块，**带 `// 自定义：` 注释**，冲突易解决
2. 若上游重构了命令注册方式或 fileTree.toggle 命令的 id/keybind，需对齐新的写法
3. 命令 id `fileTree.toggle` 必须与 `use-session-commands.tsx` 一致，`command.register` 内部会去重（`seen.has(opt.id)` 取首次注册），不会与普通会话页面冲突（两个页面不同时挂载）

---

### 1.12 删除两个会话页面的"附加文件"图标按钮（2026-07-19）

**背景**：`prompt-input.tsx` 中两处 `<TooltipKeybind>` 包装的 `data-action="prompt-attach"` 图标按钮（icon="plus"）分别在普通会话页面（L1578-1595）和新建会话页面（L1803-1821）渲染"附加文件"按钮。用户要求删除这两处可见图标按钮，但保留 `file.attach` 命令注册（快捷键 `mod+o` 仍可调起附件选择器，见 L500-501）。

| 文件 | 改动 |
| --- | --- |
| `packages/app/src/components/prompt-input.tsx` | 删除普通会话页面的 `<TooltipKeybind>` + `<IconButton data-action="prompt-attach">` 块（原 L1578-1595，约 18 行）；删除新建会话页面的 `<TooltipKeybind>` + `<Button data-action="prompt-attach">` 块及其外层包装 div（原 L1795-1823，约 29 行）。两处均保留 `// 自定义：` 注释标明删除位置 |

**业务原因**：用户希望通过快捷键 `mod+u`（`file.attach` 命令）或拖拽方式附加文件，无需可见图标按钮，简化界面。

**合并上游时**：
1. `prompt-input.tsx` 是上游文件，本次改动是**纯删除**两段 JSX 块，**带 `// 自定义：` 注释**，冲突易解决
2. 若上游重构了 prompt-input 布局，按"删除 prompt-attach 按钮"原则处理即可
3. **关键约束**：不要删除 `command.register("file.attach", ...)`（L500-506，keybind 是 `mod+u`）和 `fileAttachmentInput()`（L1577）调用，否则快捷键和文件输入框会失效

---

### 1.13 Docker 默认端口改为 8088:8088 与通用设置默认值调整（2026-07-25）

**背景**：用户要求 Docker 部署默认端口映射从 `80:8088` 改为 `8088:8088`，避免访问时混淆 80 端口；同时要求设置中“通用”菜单页面固定为指定默认开关状态。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `docker/build.ps1` | L19：`[int]$HostPort = 80` → `[int]$HostPort = 8088` |
| `docker/readme.md` | L1079/L1175/L1187/L1221/L1316/L1366/L1376/L1382：所有 `80:8088`、`localhost:80`、`http://localhost` 相关示例与验证地址改为 `8088:8088`、`localhost:8088` |
| `packages/app/src/context/settings.tsx` | L55：`newLayoutDesignsDefault` 从 `import.meta.env.VITE_OPENCODE_CHANNEL !== "prod"` 改为 `true`；L121：`defaultSettings.general` 新增 `newLayoutDesigns: true`；L136：`defaultSettings.notifications.errors` 从 `false` 改为 `true` |

**业务原因**：
1. Docker 默认访问地址与容器内部端口统一为 8088，用户直接访问 `http://localhost:8088` 即可。
2. 新用户首次打开时，通用设置默认状态符合用户偏好（关闭自动接受权限/显示推理摘要/展开 shell/展开编辑/Command palette/Server status；打开会话进度条/New layout/系统通知/File tree/Custom agents）。

**commits**：
- `79afd20b7` `chore(yejian): adjust general settings defaults`
- `404509d19` `chore(yejian): change Docker default host port to 8088`
- `c0ff0048b` `docs(yejian): update docker readme port references`
- `f5dde6c69` `docs(yejian): add dev log for port and settings defaults`
- `4dd0bfa67` `docs(yejian): update dev log index`
- `58e2a7bfc` `docs(yejian): update custom changes manifest`

**合并上游时**：
1. `docker/build.ps1`、`docker/readme.md` 是 v.1.17.6 自定义文件，dev 上没有，**整体重放**
2. `packages/app/src/context/settings.tsx` 是上游文件，本次只改 3 个默认值，冲突极小；若上游重构了 `defaultSettings` 结构，需手动把 `newLayoutDesigns: true` 和 `notifications.errors: true` 放到新的对应位置

---

### 1.14 Dockerfile apk 缓存参数修复与规范图集数据/帮助文档更新（2026-07-25）

**背景**：用户在本次会话前手动更新了现行规范、图集 HTML 页面以及帮助 PDF；同时 Dockerfile 中 `apk add --no-cache` 与 `--mount=type=cache` 配合无法真正复用 apk 缓存，改为 `--update --cache-dir /var/cache/apk`。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `docker/Dockerfile` | L28/L76：`apk add --no-cache ...` → `apk add --update --cache-dir /var/cache/apk ...`（配合 cache mount 复用 apk 缓存） |
| `packages/app/public/yejian/xianxingguifan.html` | 新增若干规范数据行；底部 `<script>` 增加 `DOMContentLoaded` 事件绑定搜索框 |
| `packages/app/public/yejian/zhuxingtuji.html` | 页面布局重构（flex 分屏、表头宽度、section-header）；搜索框改为 `DOMContentLoaded` 绑定 |
| `packages/app/public/yejian/yejian_help.pdf` | 二进制内容更新 |

**业务原因**：保持规范/图集静态页面数据与样式最新；Dockerfile 构建时复用 apk 缓存；帮助 PDF 内容更新。

**commits**：
- `4b27b4dca` `fix(yejian): use apk cache dir in Dockerfile`
- `4b06bc034` `chore(yejian): update xianxingguifan standards data`
- `d895a540a` `chore(yejian): update zhuxingtuji atlas page layout`
- `c3399f271` `docs(yejian): update yejian_help.pdf`
- `7c03b7760` `chore(yejian): add init-opencode-users script`

**合并上游时**：
1. `docker/Dockerfile` 是 v.1.17.6 自定义文件，dev 上没有，**整体重放**
2. `packages/app/public/yejian/*.html` / `*.pdf` 是自定义静态资源，dev 上没有，**整体重放**

---

### 1.15 忽略 Python 缓存目录（2026-07-25）

**背景**：`代码/init-opencode-users.py` 运行后生成 `代码/__pycache__/` 目录，Git 提示未跟踪，需要忽略。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `.gitignore` | 新增 `__pycache__/` 和 `*.py[cod]` 规则 |

**业务原因**：避免 Python 运行时缓存文件进入版本控制。

**commits**：
- `7c97c125e` `chore(yejian): ignore Python cache directories`

**合并上游时**：`.gitignore` 上游也存在，本次只加两行规则，冲突极小；若上游已有 Python 缓存规则，可丢弃。

---

### 1.16 `/yejian/` 静态页面 CSP 允许内联脚本（2026-07-25）

**背景**：Docker 部署后访问 `http://localhost:8088/yejian/zhuxingtuji.html` 和 `http://localhost:8088/yejian/xianxingguifan.html` 时，搜索框输入内容按回车无反应。原因是后端给 `text/html` 响应设置了 `Content-Security-Policy: script-src 'self' 'wasm-unsafe-eval'`，没有 `'unsafe-inline'`，导致页面里的内联搜索脚本被浏览器阻止执行。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/opencode/src/server/shared/ui.ts` | L15-18：新增 `YEJIAN_CSP`，允许 `script-src 'unsafe-inline'`；L60-71：在 `embeddedUIResponse` 中判断请求路径是否以 `yejian/` 开头，是则使用 `YEJIAN_CSP`，否则保持原有 `cspForHtml` |

**业务原因**：`/yejian/` 下的现行规范、图集等静态 HTML 页面包含内联搜索脚本，需要 CSP 放行。

**commits**：
- `814c8c40d` `fix(yejian): allow inline scripts in CSP for /yejian static pages`

**合并上游时**：`packages/opencode/src/server/shared/ui.ts` 是上游文件，但本次改动集中在 CSP 与 `embeddedUIResponse` 两个地方，合并时重点检查这两处是否冲突。

---

### 1.17 模型选择器按钮加宽 + 文件树忽略文件文字加黑 + 提示词优化技能（2026-08-02）

**背景**：三处独立小改动：① 新建会话页面、普通会话页面模型选择器按钮宽度不足，类似 `09MiMo-V2.5-Pro【复杂任务、成本5x、1M上下文、长周期自主任务】` 的长模型名称被 `truncate` 截断，需加宽并留余量（最终 550px）；② 文件树中被 `.gitignore` 忽略的文件用 `text-text-weaker`（#c7c7c7）显示太淡，看不清，需加黑；③ `skills.json`「其它」分组新增「提示词优化」技能，调用 `E:\AI\YEJIAN\.opencode\skills\prompt-optimization` 中的 skill。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/app/src/components/prompt-input.tsx` | 4 处模型选择器按钮 `max-w-[...]` 统一改为 `max-w-[550px]`：L1854（普通会话页面-未付费回退，原 320px）、L1890（普通会话页面-已付费 triggerProps，原 320px）、L2124（新建会话页面-未付费回退，原 220px）、L2152（新建会话页面-已付费 triggerProps，原 220px） |
| `packages/app/src/components/file-tree.tsx` | L170：被 `node.ignored` 的文件文字颜色从 `text-text-weaker`(#c7c7c7) 改为 `text-text-weak`(#8f8f8f)，加深一档；图标仍用 `--icon-weak-base` 灰色单色，与正常文件的彩色图标区分 |
| `packages/app/public/yejian/skills.json` | 「其它」分组（`id: "other"`）末尾新增「提示词优化」技能，`skillId: "prompt-optimization"`，含 tooltip/keyword/case/prompt |

**业务原因**：
1. 模型名称变长（含【复杂任务、成本5x、1M上下文、长周期自主任务】等说明），原 220/320px 宽度截断后无法识别模型，550px 可完整显示并留约 130px 余量。
2. 被 .gitignore 忽略的文件用 #c7c7c7 太淡，看不清；改为 #8f8f8f 加深一档，仍可通过灰色图标与正常文件区分。
3. 用户需要撰写/优化/审查提示词的方法论 skill，`prompt-optimization` 已存在于 `E:\AI\YEJIAN\.opencode\skills\`，挂到技能面板「其它」分组供调用。

**commits**：
- `6ac3b4e24` `fix(yejian): darken gitignored file text color in file tree`
- `620b3a625` `fix(yejian): widen model selector button to show full model name`
- `550f55e0c` `feat(yejian): add prompt-optimization skill to other group`

**合并上游时**：
1. `prompt-input.tsx` 是上游文件，本次改动是 4 处 `max-w-[...]` class 字符串替换，冲突极小；若上游重构了模型选择器按钮结构或宽度，按"加宽到能完整显示长模型名称"原则处理
2. `file-tree.tsx` 是上游文件，本次只改 1 行 class（`text-text-weaker` → `text-text-weak`），冲突极小；若上游调整了主题色 token 或 ignored 文件样式策略，需对齐
3. `skills.json` 是 v.1.17.6 自定义文件，dev 上没有，**整体重放**
4. **关键约束**：`prompt-input.tsx` 中 4 处 `max-w` 必须同步改，不能只改部分页面，否则两个会话页面模型选择器宽度不一致

---

### 1.18 默认模型优先 scnet + 模型中文思考指令 + 模型选择弹窗加宽（2026-08-27）

**背景**：三处独立需求：① 会话页面默认模型从「opencode zen 的第一个模型」改为「scnet（超算互联网）下的第一个模型」；② 模型思考过程是英文，希望改为中文思考（项目规则 AGENTS.md 只约束输出、约束不到模型内部思维链）；③ 模型选择弹窗加宽，好完整显示 scnet 长模型名（如「01DeepSeek-V4-Flash【普通任务、成本1x、1M上下文…】」）。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/app/src/context/local.tsx` | `defaultModel()`（L159-172）开头新增「优先 scnet」逻辑：`providers.connected()` 中命中 id `"scnet"` 时直接返回其第一个模型 `scnet/DeepSeek-V4-Flash`，否则回落原遍历逻辑（带 `// 自定义：` 注释） |
| `packages/opencode/src/session/prompt/default.txt` | 顶部新增 `请始终使用简体中文思考和回答，你的思维过程也用中文。`（首行） |
| `packages/opencode/src/session/prompt/anthropic.txt` | 同上 |
| `packages/opencode/src/session/prompt/beast.txt` | 同上 |
| `packages/opencode/src/session/prompt/gemini.txt` | 同上 |
| `packages/opencode/src/session/prompt/gpt.txt` | 同上 |
| `packages/opencode/src/session/prompt/kimi.txt` | 同上 |
| `packages/opencode/src/session/prompt/codex.txt` | 同上 |
| `packages/opencode/src/session/prompt/trinity.txt` | 同上 |
| `packages/app/src/components/dialog-select-model.tsx` | L144 模型选择弹窗尺寸 `w-72 h-80`（288×320px）→ `w-[600px] h-[400px]`（中途先到 440px，最终按用户要求 600×400） |

**关键认知**：
1. 默认模型实际选择顺序是 `configuredModel() ?? recentModel() ?? defaultModel()`（[local.tsx](file:///d:/AI/opencode/packages/app/src/context/local.tsx) L175）。只要手动点过模型（recent 记录）或有配置 `model`，scnet 不会生效；想稳定默认走 scnet 需新开会话且未手动指定过模型。
2. 思考过程是模型内部生成的思维链，模板指令只能「引导」、不保证 100% 中文；对 DeepSeek 这类中文原生模型，用中文写指令效果更好。
3. 8 个模板首行几乎都不同，但 default/trinity 与 anthropic/codex 首行字符串互相相同——编辑时只需保证**单个文件内**首行唯一即可（Edit 按文件域校验）。

**合并上游时**：
1. `local.tsx` 是上游文件，改动集中在 `defaultModel()` 开头一段独立逻辑，**带 `// 自定义：` 注释**，冲突易解决
2. `dialog-select-model.tsx` 是上游文件，只改 1 行 class 字符串，冲突极小
3. `packages/opencode/src/session/prompt/*.txt` 8 个模板是上游文件，本次只在各文件顶部加 1 行中文指令，每处几乎无冲突；但上游持续迭代模板首行内容时需逐个核对指令是否被覆盖

---

### 1.19 修复 subagent 崩溃：MessageV2.get 恢复上游水合逻辑（2026-08-30）

**背景**：v1.17.6 多用户版调用 subagent（task 工具）必现报错 `undefined is not an object (evaluating 'L.info.role')`（编译后变量名）。排查确认**不是上游 bug**：多用户改造把 `Database.Service` 重命名为 `DatabaseService` 时，`MessageV2.get` 里误插了一个提前 `return`，直接返回数据库原始行（消息数据在 `row.data` 列里，原始行没有 `info` 属性），导致调用方 [task.ts](file:///d:/AI/opencode/packages/opencode/src/tool/task.ts) L164 访问 `msg.info.role` 崩溃；`NotFoundError` 判空同时变成死代码。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/opencode/src/session/message-v2.ts` | L518-531 `get` 函数去掉多余 `return`，恢复上游 v1.17.6 原版结构：查行 → `info(row)` 水合为 `{ info, parts }` → 恢复 `NotFoundError` 判空。保留 `DatabaseService` 命名 |

**影响面**：全代码库仅 2 处调用 `MessageV2.get`——task.ts（subagent，本次崩溃点）和 HTTP 消息详情端点 `handlers/session.ts` L164（前端用可选链静默吞掉了畸形数据，该接口也一并修好）。

**验证**：与 v1.17.6 逐行 diff 确认 `get` 逻辑一致（仅剩命名差异）；`bun test test/tool/task.test.ts` 18 pass / 0 fail；Docker 镜像 v0.1.5 重建部署。

**关键认知（踩坑）**：
1. `Database.Service` → `Service as DatabaseService` 这类批量改名时，**"提前 return 消错"是高危写法**——它让后续代码全部变成不可达死代码，TS 不报错（unreachable code 不在 typecheck 默认检查里），运行期才炸。
2. 编译二进制里变量名被重命名（`L.info.role`），报错无法直接 grep 源码定位，需靠调用链推断 + 源码模式复现。
3. 同名改动的其他文件建议做一次"return 后死代码"普查（`git diff v1.17.6 -- packages/opencode/src` 人工过一遍）。

**合并上游时**：该文件 `get` 函数已与上游完全一致，合并零冲突；若上游后续重构 `get`，以覆盖的方式解决即可。

---

### 1.20 子 agent 会话归属继承 + 权限检查空值保护 + 容器时区东八区（2026-09-01）

**背景**：两个线上问题：① 子 agent 会话（task 工具在服务端内部创建，不经过 HTTP create 接口的 `writeOwnership` 补写）的 `user_id` 始终为空，普通用户在会话列表（`WHERE user_id = ?` 过滤）和详情页（`requireSession` 拒绝）双重不可见，点任务卡片跳转"新建会话"页；② 容器默认 UTC 时区，system prompt 里的日期、日志、默认会话标题时间偏慢 8 小时。线上数据库 35 条子会话已通过 SQL 迁移补齐归属（备份 `opencode.db.bak-20260901`，迁移时顺带 REINDEX 修复了损坏的 `session_user_id_idx` 索引），运行容器已加 `TZ=Asia/Shanghai` 重建。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/opencode/src/tool/task.ts` | L142-148：`sessions.create` 新增 `userID: parent.user_id`，子会话继承父会话归属 |
| `packages/opencode/src/session/session.ts` | L484-485：`Interface.create` 类型声明补 `userID?: UserID`（实现层 createNext 原本就支持，仅接口类型缺失） |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts` | L90-95：`requireSession` 的 `denied` 条件改为 `!info.user_id \|\| info.user_id !== user.value.id`，空归属显式 fail-closed |
| `docker/Dockerfile` | runtime 阶段（alpine:3.20）新增 `ENV TZ=Asia/Shanghai`；`apk add` 列表加 `tzdata` |

**业务原因**：
1. 子 agent 会话不继承归属则普通用户永远看不到子 agent 工作成果（评估报告：`evaluation-timezone-subagent/`）。
2. 存量空归属已迁移完毕，fail-closed 可防止未来异常产生的无主会话对所有登录用户可见（越权风险）；与 dev 分支"空值放行"语义不同，是有意为之的更安全选择。
3. 东八区时区让 system prompt 日期、日志、会话默认标题时间与本地一致。

**验证**：typecheck 基线对比——改动前后相关文件错误数完全一致（35=35），未引入新错误。

**commits**：
- `73271641f` `fix(yejian): inherit parent session ownership for subagent sessions and set container timezone`
- `cb3861cba` `docs(yejian): record subagent ownership and timezone fix in manifest and dev log`

**合并上游时**：三个 TS 文件都是上游文件且改动集中（create 调用处一行、接口类型两行、denied 条件一处），冲突面小；Dockerfile 是自定义文件整体重放，无需关注。

**线上生效说明**：代码改动需重建二进制 + 镜像后生效；当前线上行为已由数据迁移（子会话归属）+ 容器 TZ 重建覆盖，无需紧急发版。

### 1.21 多用户自定义代码全量 typecheck 修复（2026-09-04）

**背景**：多用户自定义代码（server 包 17 个 + opencode 包若干存量错误）历史上从未通过 `bun typecheck`，`git push` 的 husky pre-push 钩子（`bun turbo typecheck`）被挡。本批修复全部存量类型错误，23 个包 typecheck 全绿，打通推送。

**改动文件**（核心服务 Layer 化重构 + 中间件 provides 声明 + handler 错误通道映射 + 测试依赖补齐）：

| 文件 | 改动 |
| --- | --- |
| `packages/core/src/user/sql.ts` | UserTable.id 去掉 `$type<ID>()` brand 注解（drizzle 推断冲突） |
| `packages/core/src/user/password.ts` | scrypt `params` 对象显式类型注解 |
| `packages/core/src/user/index.ts` | 整体重构为 `Layer.effect` 构建时捕获 db（上游 session/store.ts 模式），各方法 R=never |
| `packages/core/src/auth-token/index.ts` | 同上 Layer.effect 重构；顺带修 `cleanupExpired` 的 `gt`→`lt`（原逻辑删的是未过期 token，方向反了） |
| `packages/server/src/middleware/auth.ts` | `CookieAuth` 声明 `provides: CurrentUser`；`HttpServerResponse` 改限定名类型 |
| `packages/server/src/middleware/authorization.ts` | `Authorization` 同样声明 `provides: CurrentUser` + 类型修正 |
| `packages/server/src/middleware/require-admin.ts` | `CurrentUser` 改 `Effect.serviceOption` 读取（缺失按非 admin fail-closed 返回 403） |
| `packages/server/src/handlers/admin.ts` | `updateUser` 失败 `mapError` 为 `InvalidRequestError` |
| `packages/server/src/handlers/auth.ts` | `changePassword` 失败映射；delete-account 返回 `{ ok: true }`（原误返回 `HttpApiSchema.NoContent` schema 对象） |
| `packages/server/src/handlers/credential.ts` / `handlers/session.ts` | `user.id` 用 `UserID.make()` 补 brand；session.get 修 `const session` 遮蔽外层服务 + TDZ 自引用（原调用即抛 ReferenceError，接口完全不可用） |
| `packages/server/src/routes.ts` | Layer.provide 顺序改回与上游一致：先 handlers（引入 RequireAdmin key 依赖）再中间件实现层 |
| `packages/opencode/src/cli/cmd/serve.ts` | 去掉 effect-smol 不支持的 `Effect.gen("name", fn)` 名字参数；bootstrapAdmin/bootstrapUsers 加 `Effect.catch` 兜底（失败打日志不阻塞启动，与种子文件 per-user catch 语义一致） |
| `packages/opencode/test/session/prompt.test.ts` | SessionPrompt 新依赖 `User.Service`（system prompt 注入登录用户），mock 掉 |
| `packages/opencode/test/server/httpapi-{authorization,control-plane,global}.test.ts` | authorizationLayer 依赖 User/AuthToken（cookie 认证），测试请求不带 cookie，`Layer.mock` 补齐 |
| `packages/app/src/components/file-tree.tsx` | classList 重复 key `"text-text-weak"` 合并为 `ignored \|\| !active()`（重复 key 后者覆盖前者，ignored 文件高亮时丢弱化样式） |
| `packages/app/src/pages/session/message-timeline.tsx` | 移除 virtua 0.49.1 `VirtualizerHandle` 上不存在的 `measure()` 调用（运行时 TypeError 中断底部锚定） |

**业务原因**：typecheck 是 pre-push 钩子的硬性门槛；且本批顺带修出 3 个真实运行时 bug（session.get TDZ、delete-account 返回值、file-tree 重复 key）。

**验证**：仓库根 `bun typecheck` 23/23 包全绿。

**commits**：`01c09ba88c`…`e5f4b492bd` 共 19 个分文件提交（core 4 / server 中间件 3 / handlers 4 / routes 1 / opencode 5 / app 2 / gitignore 1，均 `fix|refactor|test|chore(yejian)` scope）。

**合并上游时**：core 两个服务文件是自定义文件整体重放；server middleware/handlers、opencode 测试、app 两个文件是上游文件小改动（每文件 ≤ 8 行），冲突面小。

### 1.22 会话隔离：SSE 事件归属过滤 + 端点归属检查（2026-09-05）

**背景**：会话列表两条数据通道过滤规则不一致——HTTP 查询按 user_id 过滤、SSE 实时事件零过滤广播，导致用户 A 进行中的会话 title 实时泄露给全体用户且可点击跳转，会话结束后刷新又"消失"。深挖发现 `/event`、`/api/event` 两个 SSE 端点及 `sync.history/replay/steal`、`session.diff/message`、V2 `session.prompt/compact/wait/context` 全部缺归属检查（`sync.history` 原返回整张事件表，连历史消息正文都可拉）。此前 6 个未推送修复提交经实测无效已整体废弃，本次推倒重做。

**方案**：A=三个 SSE 端点按登录用户过滤（核心）；B=前端 reducer 归属兜底（第二道防线）；C=补齐端点归属检查。语义统一 fail-closed：admin 全放行（满足"admin 实时看到所有人会话事件"需求）；非 admin 只放行自己会话的会话域事件（session./message./todo./permission./question. 前缀）；未鉴权/查不到归属一律拒绝；非会话域事件不受影响。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `packages/core/src/session/ownership.ts` | **新建 101 行**（见第 2 节） |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/global.ts` | L35-49 `guardInput` 归一化 GlobalBus payload（普通事件/sync 事件双形态）；L52-58 连接时解析一次 CurrentUser；L81-88 `Stream.filterEffect` 按归属过滤 `/global/event` |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/event.ts` | L31-38 `/event` 同样过滤（原仅按 directory 过滤；CLI Basic Auth 用户授权层归 admin 不受影响） |
| `packages/server/src/handlers/event.ts` | `/api/event`（V2 SSE）复用 core 模块过滤（原仅按 location 过滤） |
| `packages/app/src/context/global-sync/event-reducer.ts` | L93-103 `ownedByCurrentUser`；L130/L146/L176 三处 session.created/updated/deleted 归属不匹配即丢弃（双边缺省放行兼容未登录/测试） |
| `packages/app/src/context/server-sync.tsx` | L7/L91-92/L413-415 取当前用户身份传入 reducer |
| `packages/app/src/context/global-sync/event-reducer.test.ts` | +66 行归属校验专项测试 |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts` | `session.diff`/`session.message` 补 `requireSession` |
| `packages/opencode/src/server/routes/instance/httpapi/groups/session.ts` | diff 端点声明加 error schema |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/sync.ts` | +94 行：`sync.history` 按用户过滤事件行、`sync.replay` 逐事件校验、`sync.steal` 迁移前校验 |
| `packages/server/src/handlers/session.ts` | L21-43 `assertSessionOwnership`（非 admin 不匹配 → 404 不泄露存在性）；V2 prompt/compact/wait/context 四端点调用 |

**业务原因**：会话隔离是多用户部署的硬性安全要求；两通道规则不一致直接导致隐私泄露。

**验证**：core/server/opencode/app 四包 typecheck 全绿；event-reducer 单测 15/15（含新增用例）；`httpapi-authorization` 等测试失败经基线 `ca070aedef` 对照确认为预存在环境问题。双用户浏览器级冒烟待用户执行；Docker 需 `-ForceRebuild` 重建生效。

**commits**（8 个，由旧到新）：`fa4f356bc0`（core 模块）→ `9233fce9f3`（/global/event）→ `0a8c76b2a5`（/event）→ `e2de00cf51`（/api/event）→ `b955d040a0`（前端兜底）→ `95c4f9c239`（diff/message）→ `1eadf60684`（sync 三端点）→ `3abc092bf4`（V2 四端点）。

**合并上游时**：上游若重构 SSE handler 或 GlobalBus payload 结构，`global.ts` 的 `guardInput` 归一化与 filterEffect 插入点是重点盯防对象；`sync.ts` 改动量大（+94 行），上游改 sync handler 结构时需手动重放归属校验逻辑。

### 1.23 Docker 镜像 v0.1.6：skill 依赖更新（2026-09-05）

**背景**：`E:\AI\YEJIAN\.opencode\skills` 9/4 重装一批 skill（新增 pptx/docx/pdf/xlsx/skill-creator 全家桶、playwright-mcp-downloader、ppt-master 新版），镜像依赖需对齐；同时 v0.1.6 打包带入 20260905 会话隔离修复与 20260904 typecheck 修复批。20260830~0905 日志复核结论：TZ/tzdata 已内置，其余均为代码修复，无新增系统依赖。

**改动文件**：

| 文件 | 改动 |
| --- | --- |
| `docker/requirements.txt` | +`python-docx>=1.1.0`（online-to-md Word 自动拆分，MinerU 200 页限制） |
| `docker/requirements-ppt.txt` | +PyYAML/XlsxWriter/skia-pathops/uharfbuzz（对齐新版 ppt-master：模板注册/表格物化/原生 DrawingML 路线）；-svglib（旧方案）-reportlab（与 requirements.txt 重复） |
| `docker/Dockerfile` | npm 全局包 3→8：+`pptxgenjs react-icons react react-dom sharp`（pptx skill PPT 生成 + 图标渲染链路；sharp 自动选 musl 预编译二进制） |
| `docker/build.ps1` | 默认 ImageTag/ContainerName 升 v0.1.6；docker run 模板加 `-e TZ=Asia/Shanghai` 对齐用户标准启动命令 |

**业务原因**：skill 运行时挂载在 `/YEJIAN` 数据卷，镜像只负责依赖环境；新 skill 缺依赖会运行时报 `Cannot find module` / `ModuleNotFoundError`。

**验证**：容器 Up、HTTP 200、时区 CST、npm 8 包 require 全过（sharp 实测渲染 PNG 成功）、Python 10 包 import 全过、`opencode --version` 1.17.6 不挂起。

**commits**：`83caf11617`（requirements.txt）→ `55de2c874e`（requirements-ppt.txt）→ `60a794b907`（Dockerfile）→ `9589f1bc31`（build.ps1）；tag `docker-v0.1.6`。

**合并上游时**：docker/ 目录全部为自定义文件，无冲突风险；注意 build.ps1 **必须保留 UTF-8 BOM**（Edit 类工具重写会丢 BOM，PS 5.1 按 GBK 误读中文注释会吞换行符报语法错误，详见当日开发日志踩坑记录）。

**遗留**：docx skill 的 pandoc 未装（可选路径，mammoth 已覆盖）；npm 全局包容器内使用需 `NODE_PATH=/usr/local/lib/node_modules`（Alpine prefix 是 /usr/local，node 默认不解析全局路径）。

---

## 2. 新建文件（低风险点）

| 文件 | 用途 |
| --- | --- |
| `日志/20260711开发日志-v0.1.6多用户分支Schema.Defect适配.md` | 本次适配的详细开发日志 |
| `日志/20260711开发日志-前端dev-proxy端口改回4096.md` | 端口还原的详细开发日志 |
| `日志/20260711开发日志-docker打包前scrypt加bootstrapUsers.md` | Docker 打包前适配的详细开发日志 |
| `日志/20260712开发日志-docker-v0.0.6多用户+ppt-master.md` | Docker v0.0.6 打包的详细开发日志 |
| `日志/20260713开发日志-docker-v0.0.8-xd-node修复.md` | Docker v0.0.8 xd.node undefined 修复的详细开发日志 |
| `日志/20260717开发日志-源码直跑模式启动修复.md` | 源码直跑模式启动修复的详细开发日志 |
| `日志/20260717开发日志-titlebar帮助按钮.md` | titlebar 帮助按钮的详细开发日志 |
| `日志/20260725开发日志-Docker默认端口与通用设置默认值调整.md` | Docker 默认端口与通用设置默认值调整的详细开发日志 |
| `日志/20260725开发日志-yejian静态页面CSP内联脚本修复.md` | `/yejian/` 静态页面 CSP 内联脚本修复的详细开发日志 |
| `日志/20260802开发日志-模型选择器加宽+文件树忽略文件加黑+提示词优化技能.md` | 模型选择器加宽 + 文件树忽略文件加黑 + 提示词优化技能的详细开发日志 |
| `代码/init-opencode-users.py` | 初始化 opencode.db 用户表的 Python 脚本（读取白名单、创建普通用户和管理员、生成 scrypt PHC hash） |
| `packages/core/src/user/password.ts` | 从 user/index.ts 拆出的 scrypt 密码哈希叶子模块（打破循环依赖） |
| `packages/core/src/session/ownership.ts` | 会话域事件归属过滤模块（`sessionEventGuard`：前缀判定 + SessionTable.user_id 查询缓存 + admin 放行 + fail-closed），供三个 SSE 端点复用 |
| `日志/20260905开发日志-会话隔离SSE事件归属过滤修复.md` | 会话隔离 SSE 归属过滤修复的详细开发日志 |
| `日志/20260905开发日志-Docker镜像v0.1.6打包skill依赖更新.md` | Docker 镜像 v0.1.6 打包（skill 依赖更新）的详细开发日志 |
| `packages/app/src/components/help-button.tsx` | 顶栏帮助按钮组件（点击打开 yejian_help.pdf） |
| `packages/opencode/src/session/user-context.ts` | 用户实名上下文格式化助手（`formatUserContext`，拼成 `<current_user>` 块注入 system prompt） |
| `docker/seed-users.json` | 多用户预置清单（admin: yejian/Yejian2016） |
| `docker/requirements-ppt.txt` | ppt-master skill 的 17 个 Python 依赖 |
| `自定义改动.md` | 本文档 |

**commits**：
- `76bccd4e2` `docs(yejian): log v0.1.6 era Schema.Defect() adaptation`
- `551d2124e` `docs(yejian): add 自定义改动.md manifest for v0.1.6 era`

---

## 3. 删除/重命名的上游文件

无。

---

## 4. 编码规范（避免 binary 编译模式 vs 源码直跑模式差异）

**背景**：本项目有两套运行模式——

- **源码直跑**（本地开发）：`bun run --conditions=browser ./src/index.ts serve`，标准 ESM 同步求值
- **binary 编译**（Docker 生产）：`bun build --compile` 后跑编译产物，模块体被包成 lazy 函数，首次访问导出才执行

两种模式对**循环依赖**和**模块顶层 `export const`** 的处理不同：源码模式会触发 TDZ（`Cannot access 'X' before initialization`），binary 模式可能绕过 TDZ 但引发 `undefined` 访问（如 `xd.node undefined`）。历史上踩过多次坑（见 1.6 节、坑 17）。

**硬性规则**（新增/修改模块时必须遵守）：

### 4.1 消除循环依赖（最根本）

循环依赖是所有 binary/源码模式差异问题的温床。

**检查方法**：

```bash
bunx madge --circular --extensions ts packages/core/src packages/opencode/src
```

**修复方法**：把**共享的底层代码**（如 `hashPassword`、`loadAllowedNames`、纯函数工具）拆到**叶子模块**（不 import 项目内其他业务模块），让业务模块单向依赖叶子模块。

**参考案例**：`packages/core/src/user/password.ts`（从 `user/index.ts` 拆出 scrypt 代码，打破 `database.ts → migration.ts → seed_initial_users.ts → user/index.ts → database.ts` 循环）。

### 4.2 自引用 `export * as X from "./self"` 必须放文件第 1 行

如果模块需要自引用命名空间导出（绕 binary lazy loading 的 `undefined` 问题），**必须放在文件最顶部**，在所有 import 之前。放末尾会在源码模式下触发 TDZ。

```ts
// ✅ 正确：放第 1 行，binding 在模块体执行前就声明可用
export * as Database from "./database"

import { ... } from "..."
// ...模块体...

// ❌ 错误：放末尾，源码模式 TDZ
import { ... } from "..."
export const node = ...
export * as Database from "./database"
```

**参考案例**：`packages/core/src/database/database.ts`、`packages/core/src/global.ts`、上游 dev 分支 `database.ts`。

### 4.3 顶层 `export const` 不要触发跨模块调用

模块顶层的 `export const` 会在模块求值时立即执行，如果它调用了函数且该函数内部 import 其他业务模块，可能触发循环依赖。

```ts
// ❌ 高风险：顶层调用函数 + 引用其他模块的导出
export const node = LayerNode.make(layerFromPath(path()), [Database.node])

// ✅ 安全：改成 lazy getter 或函数，延迟到运行时才求值
export const getNode = () => LayerNode.make(layerFromPath(path()), [Database.node])
```

定期对比上游 dev 分支的写法（如上游用 `makeGlobalNode(...)` 替代 `LayerNode.make(...)`，可能就是为了统一处理此问题）：

```bash
git diff origin/dev -- packages/core/src/database/database.ts
```

### 4.4 本地开发坚持源码模式，binary 只用于生产

- **本地**：`bun run --conditions=browser ./src/index.ts serve`（源码模式）—— 能及早发现 TDZ、循环依赖
- **Docker/生产**：`bun build --compile` 后跑 binary

不要为了"跟生产一致"在本地也跑 binary，那样改代码要重新 build，开发体验差，且掩盖问题。源码模式能跑通是 binary 能跑通的**必要条件**（但非充分条件，binary 模式还需单独验证）。

### 4.5 binary 构建后必须做冒烟测试

在 `docker/build.ps1` 或 CI 里，build 完立即跑：

```powershell
# 1. 版本检查（能捕获 lazy undefined 错误，如 xd.node undefined）
docker run --rm yejian-opencode:vX.Y.Z opencode --version

# 2. 健康检查（能捕获模块加载期错误）
docker run --rm -d -p 80:4096 --name smoke yejian-opencode:vX.Y.Z
Start-Sleep -Seconds 3
python -c "import urllib.request; urllib.request.urlopen('http://localhost/').read()"
docker stop smoke
```

binary 模式的 `xd.node undefined` 之类问题在构建时就能发现，不会带到运行时。

### 4.6 定期对齐上游 dev 的导出模式

上游 dev 分支是"参考答案"。定期 diff 关键文件，看上游怎么写导出：

```bash
git diff origin/dev -- packages/core/src/database/database.ts
git diff origin/dev -- packages/core/src/global.ts
```

如果上游改了导出模式（如 `LayerNode.make` → `makeGlobalNode`），及时跟进，能预防 binary/源码模式差异问题。

