# 自定义改动清单

> **本文件是合并上游更新时的高风险点核对清单。每次合并上游前必读。**
>
> 项目 fork 自 `anomalyco/opencode`（upstream remote 已配置）。本清单记录所有对上游的修改，按风险等级分类：
> - **高风险**：改过的上游已有文件（合并时需手动解决冲突）
> - **低风险**：新建文件（通常自动合并）
>
> 维护规则：每次提交自定义改动后同步更新本清单。详见 [项目规则](.trae/rules/project_rules.md)。

## 上游基线

- 当前上游 tag：`v1.17.13`
- 最近一次同步：2026-07-04（commit `6f91258cf chore: add upstream patches from v1.17.13 sync`）
- 下次合并上游时：先 `git fetch upstream --tags` 获取新 tag，再参照本清单逐个核对

---

## 一、改过的上游文件（高风险点 ⚠️）

合并上游时这些文件最容易冲突，需逐个 `git diff upstream-tag..HEAD -- <文件>` 检查。

### 1.1 多用户认证系统

| 文件路径 | 改了什么 | 为什么改 |
|---|---|---|
| `packages/core/src/session.ts` | v2 API 的 `Session.Service` list 加 `userID` 过滤 | 普通用户只能看自己的 session |
| `packages/core/src/session/sql.ts` | `SessionTable` 加 `user_id` 列（`text().$type<UserID>()`） | session 归属用户 |
| `packages/schema/src/session.ts` | Session schema 加 `UserID` 字段 | 传输层携带 user_id |
| `packages/protocol/src/api.ts` | 注册 `AuthGroup`、`AdminGroup`、`RequireAdmin` middleware、`UnauthorizedError`、`InvalidRequestError` | 多用户认证 API 协议 |
| `packages/server/src/api.ts` | 注册 `AdminGroup` 到 v2 Api | 后端路由挂载 |
| `packages/server/src/handlers.ts` | 注册 `AdminHandler`、`AuthHandler` | 后端业务挂载 |
| `packages/server/src/auth.ts` | 重写 `authorizationLayer`：cookie 认证 + Basic Auth 兜底；删除无密码提前 return；加 `requireAdminLayer`；加 `SESSION_COOKIE` 常量 | 多用户认证核心 |
| `packages/server/src/handlers/session.ts` | v2 API session handler 注入 `CurrentUser`，list 按 `userID` 过滤，create 写 `user_id` | session 隔离（v2 API 路径，SDK 实际不走这条） |
| `packages/opencode/src/session/session.ts` | `ListInput` 加 `userID?: UserID`；`listByProject` 加 `eq(SessionTable.user_id, input.userID)` 过滤 | **SDK 实际调用路径**，session 隔离核心 |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts` | `list` handler 注入 `CurrentUser`（普通用户传 userID，admin 看全部）；`create` handler 注入 `CurrentUser` 并补写 `user_id` | **SDK 实际调用路径**，session 隔离核心 |
| `packages/opencode/src/server/routes/instance/httpapi/middleware/authorization.ts` | 删除 `if (!ServerAuth.required(config)) return ...` 提前 return | 无密码时也注入 `CurrentUser`（否则 `yield* CurrentUser` 失败） |
| `packages/opencode/src/index.ts` | `serve` 命令加 `bootstrapAdmin` 初始化 | 启动时创建默认 admin 账号 |

### 1.2 网页端 UI

| 文件路径 | 改了什么 | 为什么改 |
|---|---|---|
| `packages/app/src/app.tsx` | 路由注册（`/login`、`/settings`、`/admin/users`、`/change-password`）；`ResolvedDraftRoute` 注入 `<SkillsPanel />` | 多用户登录页 + 技能框 |
| `packages/app/src/pages/session.tsx` | `TargetSessionPage` 注入 `<SkillsPanel />` | session 页面显示悬浮技能框 |
| `packages/app/src/pages/new-session.tsx` | 重写：用 `SessionSidePanel`（传空 review props）替代简化版 FileTree，放右侧 | new-session 页面右侧显示文件树 |
| `packages/app/src/pages/session/session-side-panel.tsx` | L216 `Show` 条件简化为 `isDesktop()`（原条件排除 new-session 页面） | 让 SessionSidePanel 在 new-session 也能渲染 |
| `packages/app/src/components/prompt-input.tsx` | placeholder 本地化为"请输入交互内容。提示：/ 调用技能；ctrl+\\ 显示文件树" | 中文提示 |
| `packages/app/src/context/layout.tsx` | fileTree 默认 tab 从 `"changes"` 改为 `"all"`（7 处默认值 + 2 处 `// yejian:` 注释） | 文件树侧边栏默认显示"所有文件"而非"更改" |
| `packages/app/vite.config.ts` | proxy 后端端口改 4096 | 对齐后端默认端口 |

### 1.3 依赖

| 文件路径 | 改了什么 | 为什么改 |
|---|---|---|
| `packages/core/package.json` | 加 `@node-rs/argon2` 依赖 | 密码哈希 |
| `bun.lock` | 更新锁文件 | 新依赖 |

### 1.4 测试

| 文件路径 | 改了什么 | 为什么改 |
|---|---|---|
| `packages/opencode/test/authorization.test.ts` | 加 mock auth services | 适配多用户 |
| `packages/opencode/test/control-plane.test.ts` | 加 mock auth services | 适配多用户 |
| `packages/opencode/test/lib/auth.ts` | 新建 mock auth services helper | 测试工具 |

---

## 二、新建文件（低风险点 ✅）

合并上游时通常自动合并，但仍需检查路径是否被上游占用。

### 2.1 多用户系统 - 后端

| 文件路径 | 用途 |
|---|---|
| `packages/schema/src/user-id.ts` | `UserID` brand 类型定义 |
| `packages/core/src/user/index.ts` | `User.Service` 实现（CRUD + 密码哈希） |
| `packages/core/src/user/sql.ts` | `UserTable` 定义（含 `UserID` brand） |
| `packages/core/src/user/allowed-names.ts` | 用户名白名单 |
| `packages/core/src/auth-token.ts` | `AuthToken.Service` 实现（session token 管理） |
| `packages/core/src/credential.ts` | `CurrentUser` credential 类型 |
| `packages/protocol/src/groups/auth.ts` | `AuthGroup` API 定义（login/logout/me/change-password） |
| `packages/protocol/src/groups/admin.ts` | `AdminGroup` API 定义（用户管理） |
| `packages/server/src/handlers/auth.ts` | 认证 handler（login/logout/me/change-password） |
| `packages/server/src/handlers/admin.ts` | admin 用户管理 handler |
| `packages/server/src/middleware/require-admin.ts` | `requireAdminLayer` middleware |

### 2.2 多用户系统 - 前端

| 文件路径 | 用途 |
|---|---|
| `packages/app/src/context/auth.tsx` | 前端认证 context + `AuthGate`（未登录跳转 `/login`） |
| `packages/app/src/pages/login.tsx` | 登录页 |
| `packages/app/src/pages/change-password.tsx` | 修改密码页 |
| `packages/app/src/pages/settings.tsx` | 设置页（含修改密码入口） |
| `packages/app/src/pages/admin/users.tsx` | 用户管理页（admin 可见） |

### 2.3 悬浮技能框

| 文件路径 | 用途 |
|---|---|
| `packages/app/src/components/skills-panel/index.tsx` | SkillsPanel 主组件（悬浮 tab + 抽屉） |
| `packages/app/src/components/skills-panel/skills-panel.css` | 样式 |
| `packages/app/src/components/skills-panel/skills-panel-types.ts` | 类型定义 |
| `packages/app/src/components/skills-panel/skills-panel-data.ts` | JSON 加载 + 校验 |
| `packages/app/src/components/skills-panel/skills-panel-state.ts` | 持久化状态 |

### 2.4 资源文件

| 文件路径 | 用途 |
|---|---|
| `packages/app/public/yejian/skills.json` | 技能元数据（7 分组 44 条） |
| `packages/app/public/yejian/skills-panel-icon.png` | 技能框入口 tab 图标 |
| `packages/app/public/yejian/*.html` | 规范数据 HTML 文件（多个） |
| `packages/app/public/LOGO.png` | 自定义 logo |
| `packages/app/public/LOGO1.ico` | 自定义 favicon |

### 2.5 Docker 部署

| 文件路径 | 用途 |
|---|---|
| `docker/Dockerfile` | 多阶段构建（编译 + 运行） |
| `docker/build.ps1` | 本地构建 + 推送 Docker 镜像脚本 |
| `docker/run.ps1` | 本地运行容器脚本 |
| `docker/requirements.txt` | Python 依赖（如适用） |
| `docker/readme.md` | 部署说明 |
| `docker/allowed-names.txt` | 允许的用户名列表 |
| `docker/api-keys.env` | 环境变量配置（含 `OPENCODE_SERVER_PASSWORD`） |

### 2.6 文档与规则

| 文件路径 | 用途 |
|---|---|
| `日志.md` | 开发日志索引（倒序） |
| `日志/*.md` | 详细开发日志（每次开发一个文件） |
| `自定义改动.md` | 本文件 |
| `.trae/rules/project_rules.md` | 项目规则（含 scope/模块化/清单维护规则） |
| `CONTEXT.md` | 项目上下文说明 |
| `yejian/` | 品牌资源目录 |

### 2.7 测试

| 文件路径 | 用途 |
|---|---|
| `packages/opencode/test/lib/auth.ts` | mock auth services helper |

---

## 三、删除/重命名的上游文件

无。

---

## 四、合并上游检查清单

每次合并上游新版本时，按以下步骤操作：

### 4.1 合并前

1. **打开本文件**，重点看"一、改过的上游文件"
2. `git fetch upstream --tags`
3. 确定要合并的上游 tag（如 `v1.17.14`）
4. 逐个检查高风险文件：`git diff v1.17.13..v1.17.14 -- <文件路径>`
   - 如果上游也改了同一文件，预判冲突点
   - 如果上游重构了文件路径，更新本清单

### 4.2 合并中

1. **阶段1（全部采纳上游）**：`git merge v1.17.14`（或 rebase）
2. 解决冲突时，优先保留上游改动，自定义改动在阶段2重新应用
3. 阶段1完成后，typecheck 确认上游代码本身能编译

### 4.3 阶段2（重新应用自定义改动）

参照本清单"一、改过的上游文件"，逐个恢复自定义改动：

1. **多用户认证**（优先级最高，按依赖顺序）：
   - `packages/schema/src/user-id.ts`（新文件，通常无需恢复）
   - `packages/core/src/user/*`（新文件）
   - `packages/protocol/src/api.ts`（注册 AuthGroup/AdminGroup）
   - `packages/server/src/auth.ts`（authorizationLayer）
   - `packages/server/src/handlers.ts`（注册 handler）
   - `packages/opencode/src/session/session.ts`（userID 过滤）
   - `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts`（CurrentUser 注入）
   - `packages/opencode/src/server/routes/instance/httpapi/middleware/authorization.ts`（删除提前 return）
   - `packages/opencode/src/index.ts`（bootstrapAdmin）

2. **网页端 UI**：
   - `packages/app/src/app.tsx`（路由 + SkillsPanel）
   - `packages/app/src/pages/session.tsx`（SkillsPanel）
   - `packages/app/src/pages/new-session.tsx`（SessionSidePanel）
   - `packages/app/src/pages/session/session-side-panel.tsx`（Show 条件）

3. **依赖**：
   - `packages/core/package.json`（argon2）
   - `bun.lock`

### 4.4 合并后

1. `bun typecheck`（每个包单独跑）
2. 前后端冒烟测试（多用户隔离 + 技能框 + 文件树）
3. 更新本清单（如果上游重构导致文件路径变化）
4. 打标签 `vX.Y.Z-yejian-多用户` 推送远程

---

## 五、关键注意事项

### 5.1 双 API 路径架构

opencode 存在两套 session API，**session 隔离必须改两处**：

| 路径 | 包 | SDK 是否调用 | 改动文件 |
|---|---|---|---|
| `/session` | opencode（InstanceHttpApi） | ✅ 前端 SDK 实际调用 | `packages/opencode/src/session/session.ts` + `handlers/session.ts` |
| `/api/session` | server（v2 Api） | ❌ 前端 SDK 不调用 | `packages/core/src/session.ts` + `packages/server/src/handlers/session.ts` |

**坑**：上一轮只改了 v2 API，普通用户仍看到全部会话。SDK 实际走 `/session`（opencode 包）。

### 5.2 V1 SessionInfo 的 user_id 硬编码 null

opencode 包的 `createNext` 函数创建 V1 SessionInfo 时 `user_id` 硬编码为 `null`（projector 不接受 user_id 字段）。需在 handler 层用 `db.update` 补写。

### 5.3 authorizationLayer 的提前 return

`packages/opencode/src/server/routes/instance/httpapi/middleware/authorization.ts` 原有 `if (!ServerAuth.required(config)) return ...` 提前 return，导致无密码时不注入 `CurrentUser`。已删除，无密码时也走 cookie 验证。

### 5.4 UserID brand 类型

`SessionTable.user_id` 列是 `text().$type<UserID>()` brand 类型，`db.update` 的 set 需要 brand 值，不能传 `string`。

### 5.5 标签命名

上游 opencode 占用了几乎所有 semver 标签（`v0.1.x` / `v0.2.x` / `v1.x`）。本项目自定义标签加后缀 `-multi-user` 或 `-yejian`，如 `v0.1.2-multi-user`。

### 5.6 PowerShell 语法

PowerShell 不支持 bash 的 `&&` 语句分隔符，用 `;` 替代。
