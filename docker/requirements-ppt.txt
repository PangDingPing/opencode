# PPT Master Skill 依赖（v0.0.6 新增）
# 来源：E:\AI\YEJIAN\.opencode\skills\ppt-master\requirements.txt
# 精简方案：用 svglib（方案 B，~5MB）替代 cairosvg（方案 A，~25MB）
# 不装 pandoc（仅处理 .docx/.html/.epub/.ipynb/.xlsx 五种原生格式）

# SVG 转 PPTX + 旁白音频
python-pptx>=0.6.21
edge-tts>=7.2.8
svglib>=1.5.0
reportlab>=4.0.0

# PDF 转 Markdown
PyMuPDF>=1.23.0

# 文档转 Markdown（.docx/.html/.epub/.ipynb）
mammoth>=1.6.0
markdownify>=0.11.6
ebooklib>=0.18
nbconvert>=7.0.0

# Excel 转 Markdown
openpyxl>=3.1.0

# 图片处理
Pillow>=9.0.0
numpy>=1.20.0

# 网页转 Markdown
requests>=2.31.0
beautifulsoup4>=4.12.0
curl_cffi>=0.7.0

# AI 图片生成（Gemini 后端）
google-genai>=1.0.0

# SVG 编辑器（本地 Web 编辑器）
flask>=3.0.0
