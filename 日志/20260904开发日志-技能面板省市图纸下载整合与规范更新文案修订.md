# 20260904 开发日志 - 技能面板省市图纸下载整合与规范更新文案修订

## 一、需求背景

用户对技能面板数据文件 `packages/app/public/yejian/skills.json` 提出 3 项调整（审查相关分组）：

1. **「市系统下载PDF」技能升级**：技能名改为「省/市系统下载PDF图纸」，prompt 从原来的"手动在终端执行 python 脚本"提示文案，改为调用 `playwright-mcp-downloader` 技能的新模板（含市/省系统、项目名、16 个专业、版本号的【】占位符）
2. **删除「省系统下载PDF」技能**：该技能原本是"功能开发中"占位项，省/市下载能力已合并进上面的新技能，占位项不再需要
3. **「规范图集自更新」技能文案修订**：case 和 prompt 统一改为"调用specification-auto-update技能对以下文件或资料提及的规范、标准数据进行更新。"（原文案是"规范审查时自动触发、无需手动调用"的说明性文字，改为可直接点用的指令模板）

## 二、备选方案

- 方案 A（采用）：直接编辑 `skills.json` 静态数据文件，技能面板读取该文件渲染，改完即生效
- 方案 B（否决）：不涉及——本次是纯数据文案调整，无代码逻辑变更，无需动组件代码

关于第 3 条的定位说明：用户需求原文未指明技能名，根据文案内容（"规范、标准数据更新"）定位到 skillId 为 `specification-auto-update` 的「规范图集自更新」技能（skills.json 审查相关分组最后一个），已向用户说明如定位有误需反馈。

## 三、文件改动

仅 1 个文件：`packages/app/public/yejian/skills.json`（+4 / -12 行）

| 位置 | 改动 |
|------|------|
| L174 | `"name": "市系统下载PDF"` → `"省/市系统下载PDF图纸"` |
| L180 | prompt 改为 `"调用playwright-mcp-downloader技能，\n\n从【市系统 / 省系统】下载 【XX项目】 的【建筑 / 结构 / 给排水 / 电气 / 暖通 / 防雷 / 绿建 / 海绵城市 / 人防 / 节能 / 道路 / 桥梁 / 岩土 / 交通 / 园林 / 景观 / 全专业】专业的【第 x 版】图纸。\n"` |
| L182-189（原） | 整块删除「省系统下载PDF」开发中占位技能（8 行） |
| L217-218 | 「规范图集自更新」的 case 和 prompt 均改为 `"调用specification-auto-update技能对以下文件或资料提及的规范、标准数据进行更新。\n"` |

注：删除省系统技能后，其后技能块行号整体上移 8 行（原「审查设计文件规范、图集」L190 → L183）。

## 四、影响范围

- **仅前端技能面板展示数据**：skills.json 是 public 静态数据，由前端 skills-panel 组件读取渲染，无后端逻辑、无代码引用
- 已用 `rg` 全仓确认：除本文件和历史日志（`日志/20260616`、`日志/20260618`）外，无任何代码硬编码引用「省系统下载PDF」或被改技能名，删除/改名无副作用
- 生效方式：本地 dev 环境刷新页面即生效；线上 Docker 环境需 `build.ps1 -ForceRebuild` 重建镜像后才可见
- 原 prompt 中的"需手动在终端执行 python review_drawing_downloader.py，联系庞定平"提示随本次修改移除——新文案默认 playwright-mcp-downloader 技能可在线直接调用

## 五、踩坑记录

1. **用户需求未指明目标技能名时的定位方法**：第 3 条需求只给了文案没给技能名，需按文案关键词（"规范、标准数据更新"）在文件内反查定位；定位后必须向用户说明定位依据，避免改错对象
2. **中文 JSON 编辑后必须校验格式**：Edit 工具直接改含 `\n` 转义的中文 JSON 字符串，容易在引号/转义上出错，改完用 `Get-Content -Raw | ConvertFrom-Json` 校验通过才算完成
3. **git diff 行数核对**：`git diff --stat` 显示 +4/-12，与预期（2 行改值 + 8 行删除 + case 改值）完全吻合，可作为快速自查手段

## 六、验证步骤

1. JSON 语法校验：`ConvertFrom-Json` 解析通过（"JSON OK"）✅
2. `git diff --stat`：4 insertions / 12 deletions，与预期改动量一致 ✅
3. 全仓 `rg` 检索技能名/skillId 引用：无代码级引用（仅历史日志提及，不影响运行）✅
4. 用户侧冒烟：本地 dev 刷新页面 → 审查相关分组 → 确认「省/市系统下载PDF图纸」显示新名称、点击后 prompt-input 写入新模板；确认「省系统下载PDF」已消失；「规范图集自更新」示例与提示均为新文案（待用户验证）

## 七、commit

- 本次 skills.json 改动**尚未提交**（等待用户确认后按 `feat(yejian): ...` / `chore(yejian): ...` 规范提交）
- 本日志文件与 `日志.md` 索引更新随同提交

## 已知遗留

- 新 prompt 引用的 `playwright-mcp-downloader` 技能需确认已在运行环境可用（本仓库 skills.json 只是面板入口文案，实际技能由运行环境侧提供）
- 线上 Docker 环境需重建镜像后技能面板才会显示本次调整
