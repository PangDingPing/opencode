# 2026.06.16 网页端悬浮技能框 - 修改说明

本次修改在 opencode 网页端 session 类页面（`/new-session`、`/:dir/session/:id?`）的最左侧、垂直方向中间位置，新增一个**悬浮技能框**入口与抽屉，整合冶建日常办公 / 审查 / 党群 / 经营 / 财务 / 多模态 / 其它 7 大类共 40+ 个技能入口；同时把 prompt-input 的占位文字本地化、并把"调用技能"作为核心交互。

## 一、前置操作

1. 沿用 6-15 已创建的分支 `update-logo`（6-15 提交了 `a7556b671 chore(app): use local LOGO1.ico for system notification icon`），未切换

```
git checkout update-logo
```

## 二、修改文件清单

### 1. 新增 `<SkillsPanel />` 组件模块（5 个新文件）

**位置**：`packages/app/src/components/skills-panel/`

#### 1.1 `index.tsx`（主组件）

- 入口按钮 `<button class="skills-panel-tab">`：贴左、垂直居中、显示 `技能库.png`；hover 抽屉滑出
- 抽屉 `<div class="skills-panel-drawer" role="dialog">`：宽 320px、白底、固定高度 `calc(100vh - 96px)`，右侧细滚动条
- 手风琴：每组点击切换 `state.expanded[group.id]`，组间互不影响
- Tooltip：用 `@opencode-ai/ui/tooltip`（`contentClass="skills-tooltip-content"` 强制深色底 + 白字），显示技能名 / 说明 / 调用关键词 / 使用案例
- 点击技能：根据 `skill.action` 分发到 `runSkillAction`
  - `"prompt"`（默认）：清空 `<div data-component="prompt-input">` → 插入 `skill.prompt` 文本节点 → 派发 `InputEvent('input', { bubbles: true })` → 收起抽屉
  - `"url"`：调 `window.open(skill.url, "_blank", "noopener,noreferrer")` → 收起抽屉
- 占位（`developing: true`）技能：`<button disabled>` + CSS `skills-item--developing`（30% 灰） + 点击提前 return
- 关闭逻辑绑在 root 的 `onMouseLeave`，用 `e.relatedTarget` 检查是否仍在 root 内部，避免鼠标移到抽屉上时误关
- JSON 加载失败：`<Show when={data()}>` 短路，不弹错误

#### 1.2 `skills-panel.css`（样式）

- 抽屉：白底 + 1px 浅边框 + 阴影
- 文字配色：分组名 100% 黑 + font-weight 600；可用技能 85% 黑；占位技能 30% 黑（`rgba(0, 0, 0, 0.85)` / `rgba(0, 0, 0, 0.3)`）
- 抽屉 tab：36×192 高瘦白底卡片，hover 略变宽（40）
- Tooltip 容器：`.skills-tooltip-content` 强制 `background-color: #1e1e1e` + 白字 + 圆角 + 边框（`!important` 覆盖 Kobalte 默认）
- 自定义滚动条（webkit）

#### 1.3 `skills-panel-types.ts`（类型定义）

```ts
export type SkillAction = "prompt" | "url"
export type Skill = {
  name: string
  developing: boolean
  skillId?: string
  tooltip: string
  keyword: string
  case: string
  prompt: string
  action?: SkillAction   // 默认 "prompt"
  url?: string           // action === "url" 时使用
}
export type SkillGroup = { id: string; name: string; skills: Skill[] }
export type SkillsData = { version: number; groups: SkillGroup[] }
export type SkillsPanelState = { open: boolean; expanded: Record<string, boolean> }
```

#### 1.4 `skills-panel-data.ts`（JSON 加载 + 校验）

- `fetch("/yejian/技能.json", { cache: "no-cache" })` 拿数据
- `isSkill` / `isSkillGroup` / `isSkillsData` 三层结构校验；非默认 22px 倍数的长宽 / 错误结构会抛出
- `isSkill` 同时允许可选的 `action` / `url` 字段

#### 1.5 `skills-panel-state.ts`（持久化）

```ts
const STORAGE_KEY = "skills-panel"
const LEGACY_KEYS: string[] = ["skills-panel.v1"]
return persisted(
  Persist.global(STORAGE_KEY, LEGACY_KEYS),
  createStore<SkillsPanelState>({ open: false, expanded: {} }),
)
```

抽屉 open 状态 + 各分组 expanded 状态都写入 `opencode.global.dat`，刷新页面后保持。

### 2. 路由注入（2 个文件）

**思路**：只在**有 prompt-input 的页面**显示悬浮框。首页 `/` 没有 prompt-input，所以不显示。

#### 2.1 `packages/app/src/app.tsx`（`/new-session` 路由）

- `import` 区加 `const SkillsPanel = lazy(() => import("@/components/skills-panel"))`
- `ResolvedDraftRoute` 内 `DirectoryDataProvider` 末尾加 `<SkillsPanel />`

```tsx
return (
  <Show when={directory()} keyed>
    {(dir) => (
      <SDKProvider directory={dir}>
        <DirectoryDataProvider directory={dir} draftID={props.draftID}>
          <DraftProviders>
            <NewSession />
          </DraftProviders>
          <SkillsPanel />
        </DirectoryDataProvider>
      </SDKProvider>
    )}
  </Show>
)
```

#### 2.2 `packages/app/src/pages/directory-layout.tsx`（`/:dir/session/:id?` 路由）

- `import { SkillsPanel } from "@/components/skills-panel"`
- `Layout` 的 return 把 `<SkillsPanel />` 移到 `<Show>` 外层（用 Fragment 包），保证 `params.dir` 解析前就已经渲染，不受 `resolved()` 异步状态影响

```tsx
return (
  <>
    <SkillsPanel />
    <Show when={resolved()} keyed>
      {(resolved) => (
        <SDKProvider directory={resolved}>
          <DirectoryDataProvider directory={resolved}>{props.children}</DirectoryDataProvider>
        </SDKProvider>
      )}
    </Show>
  </>
)
```

### 3. prompt-input 占位文字本地化

**文件**：`packages/app/src/components/prompt-input.tsx`

**修改内容**：第 1338-1341 行的 `designPlaceholder` 字符串。

```ts
const designPlaceholder = () => {
  if (store.mode === "shell") return placeholder()
- return "Ask anything, / for commands, @ for context..."
+ return "请输入交互内容。提示：/ 调用技能；ctrl+\\ 显示文件树"
}
```

新占位提示用户使用 `/ 调用技能`（与本次新增的悬浮技能框对接）和 `ctrl+\` 显示文件树。

> 旧版（`placeholder()` 由 `prompt-input/placeholder.ts` 单独控制）未改，与本次新设计无关。

## 三、新增资源文件

| 目标文件 | 源文件 | 用途 |
|---|---|---|
| `packages/app/public/yejian/技能库.png` | `D:\AI\opencode\yejian\技能库.png` | 悬浮框入口 tab 图标 |
| `packages/app/public/yejian/技能.json` | （本次手写） | 7 分组 44 条技能元数据 |
| `yejian/技能库.png`（根目录） | `D:\AI\opencode\yejian\技能库.png` | 源文件备份，与副本一起入库 |

> 源文件保持原中文文件名；web 静态资源用同样中文路径，Vite 在 dev 模式与构建产物下均能直接以根路径 `/yejian/技能库.png` 暴露。

#### `技能.json` 结构

```json
{
  "version": 1,
  "groups": [
    {
      "id": "daily-office",
      "name": "日常办公",
      "skills": [
        {
          "name": "PDF转MD",
          "developing": false,
          "skillId": "online-to-md",
          "tooltip": "使用 MinerU API 将 PDF / Word / PPT / 图片精准解析为 Markdown。",
          "keyword": "/online-to-md <文件路径或URL>",
          "case": "把 D:/AI/report.pdf 转成 Markdown",
          "prompt": "请使用 online-to-md 技能，把以下文件转成 Markdown："
        }
      ]
    }
  ]
}
```

7 个分组的 skillId 映射：

| 分组 | 已开发技能（`developing: false`） | 占位技能 |
|---|---|---|
| 日常办公 | PDF转MD（online-to-md） | `*生成PPT`、`*生成提示词`、`*会议纪要`、`*公司制度查询`、`*制度数据更新` |
| 审查相关 | 绿建审查（Green-Building-Design-Review）、市系统下载PDF（review-drawing-downloader）、规范图集自更新（specification-auto-update）、**现行规范数据**（url 模式）、**现行图集数据**（url 模式） | `*节能审查`、`*总说明审查`、`*省系统下载PDF` |
| 党群文章 | 公文排版（document-format） | `*宣传稿撰写`、`*党建文章`、`*公众号一键发布`、`*文章润色`、`*文本扩/缩/改/续写`、`*文章总结`、`*公众号数据自更新` |
| 经营投标 | （无） | `*商务标生成`、`*技术标生成`、`*合同审查`、`*刺探情报`、`*招标信息`、`*经营数据看板` |
| 财务相关 | （无） | `*票据信息采集`、`*数据分析`、`*表格整理`、`*财务数据看板` |
| 多模态 | 文本转语音（edge-tts） | `*文生图`、`*图生文`、`*语音转文本`、`*视频制作`、`*视频总结` |
| 其它 | 梳理思路（thought-clarifier）、个人决策顾问（personal-decision-advisor） | `*幻觉检测`、`*头脑风暴`、`*文章去AI味` |

> 6-16 末段调整：**审查相关**分组下两项占位技能切换到 url 模式
> - `现行规范数据` → `action: "url"`, `url: "/YEJIAN/现行规范数据2026-02-04.html"`，点击新窗口打开该 HTML
> - `现行图集数据` → `action: "url"`, `url: "/YEJIAN/最新图集数据20260206.html"`，点击新窗口打开该 HTML
> 对应工具的 `prompt` 字段留空（url 模式不写 prompt-input）。

## 四、删除资源文件

无。

## 五、启动验证

修改完成后分别启动前后端进行验证。

- **后端**（在 `packages/opencode` 目录下）：
  ```
  bun run --conditions=browser ./src/index.ts serve --port 5099
  ```
  > 4096 端口在开发机本机被未知进程占用，6-15 已确认为 `EADDRINUSE`，本次沿用 6-15 的 5099 端口绕过。

- **前端**（在 `packages/app` 目录下）：
  ```
  bun dev -- --port 4444
  ```

浏览器访问 `http://localhost:4444`，按以下步骤逐项验证：

1. 首页 `/`（无 prompt-input）：左侧**不**应出现技能悬浮 tab
2. `/new-session?draftId=...`（新建会话）：左侧贴边出现 36×192 的"技能库.png"白底卡片
3. `/:dir/session/ses_xxx`（普通会话）：同样出现技能悬浮 tab
4. hover tab：抽屉向右滑出，宽度 320px、白底
5. 点击分组名（"日常办公"等）：手风琴展开，▶ 变 ▼，其它组保持原状；刷新页面后状态保持
6. hover 技能项：tooltip 出现，深色底白字，"说明："、"调用关键词："、"使用案例：" 灰白可读
7. 点击已开发技能（"PDF转MD" / "绿建审查" 等）：prompt-input 出现预设文字、抽屉收起
8. 点击"现行规范数据"：**新窗口打开** `/YEJIAN/现行规范数据2026-02-04.html`、抽屉收起
9. 点击"现行图集数据"：**新窗口打开** `/YEJIAN/最新图集数据20260206.html`、抽屉收起
10. 点击占位技能（"\*生成PPT"）：无任何写入、抽屉仍收起（disabled + return）
11. DevTools → Network：可见 `200 /yejian/技能.json` 和 `200 /yejian/技能库.png`
12. prompt-input 空状态显示 "请输入交互内容。提示：/ 调用技能；ctrl+\ 显示文件树"

## 六、附注

### 6.1 抽屉与主内容的关系

- 抽屉用 `position: fixed; left: 0; top: 50%; z-index: 9999`，覆盖在主内容之上
- 主内容容器（HomeDesign 的 280 + 720 grid）z-axis 默认不会被覆盖，视觉上不冲突
- 抽屉 tab 与左侧 sidebar（"服务器"、"项目"列表等）可能视觉重叠，暂未处理；后续若需要可在窄屏（< 1280px）自动隐藏

### 6.2 颜色分层逻辑

最终采用三档灰度（白底抽屉）：

| 元素 | 颜色 | 用途 |
|---|---|---|
| 技能分组名 | `#000` 100% 黑 | 导航锚点，最醒目 |
| 可用技能名 | `rgba(0,0,0,0.85)` 85% 黑 | 主功能区 |
| 占位（开发中）技能名 | `rgba(0,0,0,0.3)` 30% 黑 | 占位灰，明显弱于可用技能 |

> 调试过程中试过"白底黑字"和"深底白字"两种配色，最终用户选定白底三档灰。

### 6.3 url 模式扩展性

`Skill.action` 字段允许后续再扩展第三种动作（如 `action: "command"` 执行某条命令），只需要：
1. 在 `runSkillAction` 加一个分支
2. JSON 里加 `action: "command"` 和必要字段

校验（`isSkill`）和分发（`runSkillAction`）已用联合类型 `SkillAction` 兜底，新增类型需要先更新类型定义。

### 6.4 与桌面端的关系

本次**未改**桌面端：
- `packages/desktop/src/renderer/index.tsx:212` 仍硬编码 `https://opencode.ai/favicon-96x96-v3.png`
- `packages/desktop/src/main/ipc.ts:188` 仍 `new Notification({ title, body }).show()` 未传 icon

桌面端是否同步本地化由后续决定。

### 6.5 typecheck

按 [packages/app/AGENTS.md](packages/app/AGENTS.md) 跑 `bun typecheck` 通过；唯一报错是上游已存在的 `src/pages/session/message-timeline.tsx(585,18): error TS2339: Property 'measure' does not exist on type 'VirtualizerHandle'`，与本任务无关，未处理。

### 6.6 提交列表

本次共 **6 个 commit**，全部在 `update-logo` 分支上：

| # | Hash | 标题 |
|---|---|---|
| 1 | `3712dd721` | `feat(app): add floating skills panel with skill library JSON` |
| 2 | `6a3b5e86a` | `feat(app): localize prompt-input placeholder` |
| 3 | `9739de9b9` | `feat(skills-panel): extend Skill type with action and url fields` |
| 4 | `3de790f4e` | `feat(skills-panel): accept optional action/url in JSON schema` |
| 5 | `6af792989` | `feat(skills-panel): dispatch click to window.open for url skills` |
| 6 | `33837b52c` | `chore(skills-panel): switch 审查相关 规范/图集 skills to url action` |

按"代码基础 → 代码应用 → 数据消费"的依赖顺序拆 commit，每个 commit 独立可构建、可回滚。**未推送到远程**。

---

# 2026.06.16 Docker 镜像 v0.0.3 升级 - 修改说明

本次修改把 opencode Docker 镜像从 v0.0.2 升级到 v0.0.3，主要解决"Skill 运行时依赖缺失"问题（`D:\AI\AIworkbench\.trae\skills\` 下的 docx / pdf / xlsx / edge-tts 四个 skill 在容器内跑不起来），同时把基础镜像从 Alpine 3.21.3 升到 3.24.1（拿到 Node 24 LTS / Python 3.14 / pnpm 11 等新版运行时），并补上 API key 注入和局域网访问说明。

## 一、前置操作

无（基于 v0.0.2 增量升级，沿用 6-15 已创建的 `update-logo` 分支，未切换）。

## 二、修改文件清单

### 1. `docker/Dockerfile`（重写）

- `FROM alpine:3.21.3` → `FROM alpine:3.24.1`
- `RUN apk add --no-cache` 一次性装齐（按类别）：
  - 基础：`libgcc libstdc++ ripgrep xdg-utils`
  - 工具：`bash git curl ca-certificates`
  - Node 生态：`nodejs` (24 LTS) `npm` `pnpm` (11)
  - Python 生态：`python3` (3.14) `py3-pip`
  - 高速包管理器：`uv`（10~100x 比 pip 快）
  - LibreOffice 套件（必需，~300MB，docx 转换 + xlsx 公式重算）
  - PDF 工具：`poppler-utils` `poppler-data` `qpdf`
- 新增 `COPY docker/requirements.txt /tmp/requirements.txt` + `uv pip install --system --break-system-packages -r ...`：装 12 个 Python 包
- 新增 `npm install -g --silent docx pdf-lib pdfjs-dist && npm cache clean --force`：装 3 个全局 NPM 包
- 新增 `RUN opencode --version && node --version && python3 --version && uv --version` 构建期验证

**关键技巧**：`--break-system-packages` 用于绕过 Python 3.14 启用的 PEP 668 "externally-managed" 标记。Docker 镜像里使用完全安全（一次性构建、容器销毁清零、不存在破坏系统包管理器的风险）。

### 2. `docker/run.ps1`（重写）

- `param` 新增 `[string]$EnvFile = ""`（可选，传 .env 文件路径，脚本自动用 `--env-file` 注入容器）
- 把所有中文 `Write-Host` 字符串全部改成纯英文（避开 PowerShell 5.1 GBK 编码坑，见坑 1 / 6.1 坑 D）
- 启动容器拆成 **两个独立 if/else 分支**（带 envFile / 不带 envFile），避免变量内含空格被 PowerShell 当成单个参数（见 6.1 坑 C）

### 3. `docker/build.ps1`（改）

- `param` 新增 `[string]$EnvFile = ""`
- `$ImageTag` 默认值 `v0.0.2` → `v0.0.3`
- 启动容器部分同步拆成两个 if/else 分支
- 其它二进制构建逻辑保持不变

### 4. `docker/readme.md`（改）

- 顶部信息更新到 v0.0.3
- 新增 "Skill 运行时依赖" 章节（apk 包 / pip 包 / npm 包三张表 + 按需 `apk add` 说明）
- 新增 "API key 配置" 章节（`--env-file` 推荐 + 其它方式对比）
- 新增 "局域网访问配置" 章节（症状 / 原因 / `New-NetFirewallRule` 解法 / 其它可能）
- 末尾 "变更记录" 表加 v0.0.3 行
- 末尾追加"附录：v0.0.3 Docker 升级开发日志"详细章节

## 三、新增资源文件

| 文件 | 用途 |
|---|---|
| `docker/requirements.txt` | 12 个 Python 包清单，按 skill 分组注释；Dockerfile 用 `uv pip install` 装到系统 site-packages |

## 四、删除资源文件

无。

## 五、启动验证

### 5.1 镜像构建

```powershell
cd D:\ai\opencode
powershell -ExecutionPolicy Bypass -File .\docker\build.ps1 -EnvFile "D:\AI\opencode\docker\api-keys.env"
```

构建各阶段耗时（首次拉取依赖；后续 rebuild 会命中缓存）：

| 阶段 | 耗时 | 说明 |
|---|---|---|
| 拉 Alpine 3.24.1 基础镜像 | ~30s | 首次 |
| `apk add` 装所有系统包 | ~9 分钟 | LibreOffice 占大头（~300MB） |
| `uv pip install` 12 个 Python 包 | ~9s | uv 极快 |
| `npm install -g` 3 个 NPM 包 | ~35s | |
| 最终镜像体积 | ~900MB | 从 v0.0.2 的 ~150MB 增加 ~750MB |

### 5.2 启动容器

```powershell
powershell -ExecutionPolicy Bypass -File .\docker\run.ps1 -EnvFile "D:\AI\opencode\docker\api-keys.env"
```

### 5.3 容器内逐项验证

```powershell
docker exec -it yejian-AIworkbench sh

# 验证各运行时版本
node --version          # v24.x
python3 --version       # 3.14.x
pnpm --version          # 11.x
uv --version            # uv 0.x
soffice --version       # LibreOffice 信息
pdftotext -v 2>&1 | head -1    # poppler 信息
qpdf --version          # qpdf 信息

# 验证 Python 包（应全部能 import）
python3 -c "import openpyxl, pypdf, pdfplumber, reportlab, defusedxml, lxml, PIL, pandas, numpy, pypdfium2; print('Python deps OK')"

# 验证 NPM 包（应全部能 require）
node -e "require('docx'); require('pdf-lib'); require('pdfjs-dist'); console.log('NPM deps OK')"

# 验证 API key 注入
echo $ANTHROPIC_API_KEY
env | grep -i API
```

### 5.4 浏览器访问

打开 **http://localhost:8088**，应能看到 opencode Web 界面。局域网访问需先执行 `New-NetFirewallRule` 放行 8088（见 readme.md "局域网访问配置" 章节）。

## 六、附注

### 6.1 本次新增的 4 个坑

| # | 现象 | 原因 | 解决 |
|---|---|---|---|
| 坑 A | `error: The interpreter at /usr is externally managed`（uv 拒绝装包） | Alpine 3.24 自带的 Python 3.14 启用了 PEP 668，标记系统 Python 归 apk 管、拒绝 pip/uv 安装 | `uv pip install --break-system-packages`（uv 镜像了 pip 的同名参数） |
| 坑 B | `Unable to find image 'run:latest' locally`（Docker 把 `run` 当成镜像名） | PowerShell 的 `@array` splatting 与 Docker CLI 配合有问题 | 改回最原始的"反引号续行 + 字面量参数"写法 |
| 坑 C | `invalid reference format: repository name (library/ --env-file D) must be lowercase` | `$envFileArg = " --env-file \`"$EnvFile\`""` 把"flag + value"塞进一个变量，PowerShell 不会按空格拆分、整体作为一个参数传给 Docker | 把带 envFile / 不带 envFile 拆成两个独立的 if/else 代码分支，每个分支用字面量参数 |
| 坑 D | `字符串缺少终止符: "`（PowerShell 解析失败） | PowerShell 5.1 按 GBK 解析 .ps1 文件，中文字符被当乱码 | PS1 脚本内所有 `Write-Host` 字符串改用纯英文（中文只放 `#` 注释里） |

### 6.2 镜像体积增量分布

| 组件 | 大小 | 备注 |
|---|---|---|
| LibreOffice | +300MB | 必需（docx 转换 + xlsx 公式重算） |
| Python 12 包 | +200MB | pypdf / pdfplumber / openpyxl 等 |
| Python3 + py3-pip | +70MB | 基础 |
| uv | +50MB | 高速包管理器 |
| Node 3 包 | +50MB | docx / pdf-lib / pdfjs-dist |
| nodejs + npm + pnpm | +50MB | 基础 |
| poppler / poppler-data / qpdf | +15MB | PDF 工具链 |
| bash / git / curl 等 | +5MB | 通用工具 |
| **合计** | **~750MB** | v0.0.2 150MB → v0.0.3 ~900MB |

### 6.3 暂未安装的可选包（按需 `docker exec` 进去手动 `apk add`）

| 包 | 用途 | 大小 |
|---|---|---|
| `tesseract-ocr` + `tesseract-data-chi_sim` / `chi_tra` | 扫描版 PDF 做 OCR | ~100MB |
| `font-noto-cjk` | PDF 显示中文 | ~50MB |

### 6.4 局域网访问的"防火墙"问题与构建无关

`localhost:8088` 能访问、`192.168.x.x:8088` 不行，是 Windows 防火墙拦截（`docker run -p` 时 Docker Desktop 有时不会自动加防火墙规则），不是镜像本身的问题。详见 readme.md "局域网访问配置" 章节里的 `New-NetFirewallRule` 解法。

### 6.5 typecheck / lint

本次仅修改 `docker/` 目录下的 shell / markdown / Dockerfile，未触及 `packages/*` 任何 TypeScript / TSX 代码，无需跑 `bun typecheck`。

### 6.6 提交列表

**本次改动尚未 git commit**。计划按"代码基础 → 数据 → 应用"顺序拆 5 个 commit（沿用 6-15 起的 `update-logo` 分支）：

| # | 待提交文件 | 提交类型 | 标题（草稿） |
|---|---|---|---|
| 1 | `docker/Dockerfile` | `feat(docker)` | `feat(docker): upgrade to alpine 3.24.1 with full skill runtime` |
| 2 | `docker/requirements.txt` | `feat(docker)` | `feat(docker): add Python deps for docx/pdf/xlsx skills` |
| 3 | `docker/run.ps1` | `feat(docker)` | `feat(docker): add -EnvFile param for API key injection` |
| 4 | `docker/build.ps1` | `chore(docker)` | `chore(docker): bump default tag to v0.0.3` |
| 5 | `docker/readme.md` | `docs(docker)` | `docs(docker): document v0.0.3 changes, skills deps, firewall` |

每个 commit 独立可构建、可回滚。等用户确认后再执行 `git add / commit`（不主动 push）。
