# Docker v0.1.3 镜像重建（利用缓存）+ 缓存经验写入 readme

## 一、背景

上一会话已完成三个 UI/技能修改（模型选择器加宽、文件树忽略文件加黑、提示词优化技能）并提交。本会话在用户要求下：

1. 参考 `docker/readme.md`，尽量利用已缓存的依赖打包 docker image v0.1.3
2. 删除旧 v0.1.3 镜像后重新构建（不考虑旧 v0.1.3 镜像）
3. 把缓存利用经验写入 `docker/readme.md`，作为以后 Docker 构建的默认参照

## 二、改动文件清单

### 1. `docker/build.ps1`（已在前一会话修改）

- `$ImageTag` 默认值 `v0.0.9` → `v0.1.3`
- 新增 `-RebuildBuilder` 开关参数，等价于 `docker build --no-cache-filter=builder`（只重建 builder 阶段，runtime 用缓存）
- 构建命令三分支：`-ForceRebuild`（全量 `--no-cache`）/ `-RebuildBuilder`（`--no-cache-filter=builder`）/ 默认（全缓存）

### 2. `docker/Dockerfile`（已在前一会话修改）

- 新增 `ENV MODELS_DEV_API_JSON=/build/docker/models-dev-api.json`：避免 `build.ts` 联网下载 models.dev/api.json（国内无法访问）

### 3. `docker/models-dev-api.json`（已在前一会话修复）

- 通过 Bun 脚本从末尾查找有效 JSON 截断点并闭合括号，修复文件截断导致的 JSON 解析错误
- 修复后包含 zhipuai / lucidquery / anyapi 三个 provider 的模型数据

### 4. `docker/readme.md`（本次新增章节）

- 在"单独使用各脚本"和"完整构建流程"之间新增「利用缓存构建（推荐，以后默认参照）」章节
- 内容包含：三种构建模式对照表、`-RebuildBuilder` 模式原理、v0.1.3 实测缓存利用情况、5 个关键缓存挂载说明、何时该删旧镜像

## 三、构建过程

### 3.1 删除旧 v0.1.3 镜像

```powershell
# 检查是否有容器在用旧 v0.1.3 镜像
docker ps -a --filter ancestor=yejian-opencode:v0.1.3 --format "{{.Names}} {{.Status}}"
# 无输出，可直接删除

docker rmi yejian-opencode:v0.1.3
# Untagged: yejian-opencode:v0.1.3
# Deleted: sha256:5ea88ac0c1dc...
```

### 3.2 利用缓存重建 v0.1.3

```powershell
powershell -ExecutionPolicy Bypass -File "D:\AI\opencode\docker\build.ps1" -RebuildBuilder
```

`-RebuildBuilder` 等价于 `docker build --network=host --no-cache-filter=builder`：

- **builder 阶段**：忽略缓存重新编译 binary（约 30 秒），`bun install` / `npm` 下载缓存仍命中
- **runtime 阶段**：完整复用旧缓存（apk / pip / npm / uv 全部 CACHED，0 秒）

### 3.3 构建结果

| 项目 | 状态 |
|------|------|
| 旧 v0.1.3 镜像 | 已删除（5ea88ac0c1dc） |
| 新 v0.1.3 镜像 | 已构建（cda47fc23e1c, 2.07GB / 561MB） |
| 容器 yejian-AIworkbench | 已启动（端口 0.0.0.0:8088->8088） |
| HTTP 健康检查 | 200 OK |

### 3.4 缓存利用情况

| 阶段 | 状态 | 耗时 |
|------|------|------|
| builder 阶段编译 binary | 重新编译 | ~30 秒 |
| builder 阶段 `bun install` / `npm` 下载 | 缓存命中 | 快速 |
| runtime 阶段 apk / pip / npm / uv | **全部 CACHED** | 0 秒 |
| **整体构建** | — | **约 1 分钟** |

## 四、关键经验（已写入 readme.md）

### 4.1 三种构建模式

| 模式 | 命令 | 适用场景 | 耗时 |
|------|------|----------|------|
| 默认（全缓存） | `.\docker\build.ps1` | 源码未变，只换 tag 或重新打包 | ~1 分钟（runtime 全 CACHED） |
| 只重建 builder | `.\docker\build.ps1 -RebuildBuilder` | **改了源码（最常用）** | ~1 分钟（builder ~30s + runtime 全 CACHED） |
| 强制全量重建 | `.\docker\build.ps1 -ForceRebuild` | 改了 Dockerfile / 依赖 / 缓存损坏 | 1-2 小时 |

### 4.2 BuildKit 缓存挂载特性

`--mount=type=cache` 是持久化下载缓存，**删除镜像不会丢失**这些缓存（缓存存在 Docker builder 的独立存储区，与镜像层分离）：

| 挂载路径 | 作用 |
|----------|------|
| `/var/cache/apk` | Alpine apk 包下载缓存（libreoffice 300MB 等） |
| `/root/.bun/install/cache` | Bun 依赖下载缓存 |
| `/root/.cache/pip` | pip 下载缓存 |
| `/root/.cache/uv` | uv 下载缓存 |
| `/root/.npm` | npm 下载缓存 |

### 4.3 何时该删旧镜像

重建同一 tag 的镜像时，**无需手动 `docker rmi`**——`docker build` 会自动用新镜像替换旧 tag。只有以下情况需要删镜像：

- 旧镜像成为 `<none>` 悬空镜像占磁盘时，用 `docker image prune` 清理
- 想彻底确认是从 builder 阶段重新构建（用 `-RebuildBuilder` 或 `-ForceRebuild` 即可，不必删镜像）

## 五、验证

- `docker images yejian-opencode:v0.1.3`：cda47fc23e1c, 2.07GB / 561MB
- `docker ps --filter name=yejian-AIworkbench`：Up, 0.0.0.0:8088->8088/tcp
- `Invoke-WebRequest http://localhost:8088`：200 OK

## 六、待办

- [ ] 提交本次 `docker/readme.md` 改动（待用户确认后 commit，按规则不主动 push）
