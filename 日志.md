# 开发日志

## 2026-09-03

### 修复 subagent 前端报错：node_modules 混合安装导致 @solidjs/router 双实例、上下文分裂

- 背景：本地 dev 环境启动 subagent 必现 `use router primitives can be only used inside a Route`（ui 包 message-part.tsx 的 task 工具 `useLocation()` 处）。排查确认是**环境问题非代码 bug**：历史安装方式变更（isolated → hoisted linker）叠加，`@solidjs/router` 磁盘上两份物理文件——app 包解析到根目录实体副本、ui 包解析到 .bun 存储副本 → Vite 视为两模块 → 两份 RouterContextObj → `useLocation` 查不到 context 抛错。`useData` 不报错是因为 Provider/消费者同在 ui 包同一份模块。与数据库无关
- 诊断关键：`Invoke-WebRequest` 抓 Vite dev server 返回的两个源文件 import URL 对比；另注意 Solid context id 是 Symbol，`Object.keys(owner.context)` 会误报"无 context"
- 改动：**无代码改动**，纯环境修复——删除根目录 + 全部 21 个 `packages/*/node_modules`（含 1437 实体包、60 个 .old-* 残骸）→ `bun install` 干净重装（2293 包，hoisted 模式统一路径）
- 踩坑：① PowerShell Remove-Item 对个别文件报 ArgumentException 删不掉时，用 `[System.IO.Directory]::Delete(path, $true)` 可删；② bun install 的 postinstall 在沙箱内被拦导致 29 个工作区链接失败，**重跑一次即补齐**；③ 验证 Virtua 虚拟列表页面必须把 `.scroll-view__viewport` 滚到顶部（task 消息在会话中部，不滚不渲染）；④ 登录前的初始 401 是正常噪音勿误判
- 验证：两个包的 router import URL 统一为同一物理文件 ✅；Playwright 登录 → 打开原报错会话 → 滚顶 → task 卡片（"general搜索张雪机车最新新闻"）正常渲染、5 条消息完整、Router 错误 0 个（修复前必现）✅
- commits：无（纯环境修复）
- 已知遗留：前后端 dev 服务在后台运行（用户浏览器需 Ctrl+Shift+R 强刷）；查本地库用 bun:sqlite（无 better-sqlite3，part 表 JSON 列名是 data）
- 详细日志：[20260903开发日志-node_modules混合安装导致Router上下文分裂修复.md](日志/20260903开发日志-node_modules混合安装导致Router上下文分裂修复.md)

## 2026-09-01

### 子 agent 会话归属继承 + 权限空值保护 + 容器时区东八区（含分支纠错）

- 背景：两个线上问题（docker v0.1.5）：① 容器 UTC 时区，system prompt 日期/日志/默认会话标题慢 8 小时；② 普通用户打不开子 agent 会话（点任务卡片跳"新建会话"页），管理员正常。排查确认子 agent 功能本身没坏（35 条子会话都有完整消息记录），是**归属缺失的双重不可见**：子会话由 task 工具服务端内部创建、不走 HTTP create 的 `writeOwnership` 补写 → `user_id` 恒为 NULL → 会话列表 `WHERE user_id = ?` 过滤不掉 + `requireSession` 一律 404
- 修复分两段生效：**线上立即生效**（SQL 迁移 35 条子会话继承父会话归属 + 容器加 `-e TZ=Asia/Shanghai` 重建）；**代码沉淀待下次重建镜像**（4 文件 +17/-3）
  - `packages/opencode/src/tool/task.ts` L142-148：`sessions.create` 传 `userID: parent.user_id` 继承归属（治本）
  - `packages/opencode/src/session/session.ts` L484-485：`Interface.create` 类型补 `userID?: UserID`（实现层原本就支持，仅接口漏声明）
  - `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts` L90-95：requireSession 空归属显式 **fail-closed**（有意与上游 dev 的"空值放行"不同，防无主会话越权可见）
  - `docker/Dockerfile`：runtime 阶段 `ENV TZ=Asia/Shanghai` + `apk add tzdata`
- 分支纠错插曲：修复最初误做在 `multi-user-system` 分支（用户引导拉取），后确认线上 v0.1.5 的构建分支是 **`v.1.17.6多用户`**；stash 保存 → 切分支 → `git checkout stash@{0} -- <文件>` 恢复三个 TS 文件（两分支间代码完全一致）→ Dockerfile 按本分支多阶段构建单独改
- 意外发现并修复：迁移 UPDATE 报 `database disk image is malformed`，`integrity_check` 定位是 **`session_user_id_idx` 索引损坏**（主表数据完好），`REINDEX` 重建后迁移成功；数据库已在线备份至 `E:\AI\AIworkbench-data\root\opencode.db.bak-20260901`
- 踩坑：① 索引损坏伪装成库损坏，先 REINDEX 再操作；② 主机沙箱开不了 WAL 库（-shm 被拒），一律进容器用 python3；③ 重建容器必须 `docker inspect` 提取真实配置（实际挂载与 build.ps1 模板不一致）；④ 带 .ps1 中文脚本按 ANSI 解析会语法错误，脚本一律英文；⑤ 评估报告里的数字会漂移（35 条 NULL 实施时只剩 1 条），迁移脚本必须实施时重新预检
- 验证：迁移后 35 条子会话归属全对、integrity_check ok、容器 date 显示 CST、HTTP 200、typecheck 基线对比 35=35 无新增错误；**待用户冒烟**普通用户列表可见性 + system prompt 日期
- commits：`73271641f`（fix）、`cb3861cba`（docs：自定义改动.md + 日志）
- 已知遗留：代码改动待重建镜像生效；stash@{0} 待清理；multi-user-system/dev 分支同问题下次同步上游时统一处理
- 详细日志：[20260901开发日志-子agent会话归属修复+容器时区东八区.md](日志/20260901开发日志-子agent会话归属修复+容器时区东八区.md)
- 评估报告：`evaluation-timezone-subagent/evaluation-timezone-subagent.html`

## 2026-08-30

### 修复 subagent 崩溃：MessageV2.get 恢复上游水合逻辑 + Docker v0.1.5

- 背景：v1.17.6 多用户版调用 subagent 必现 `undefined is not an object (evaluating 'L.info.role')`。排查确认**不是上游 bug**（v1.17.6..v1.18.25 无对应修复）：多用户改造把 `Database.Service` 改名 `DatabaseService` 时，`MessageV2.get` 被误插提前 `return` 直接返回数据库原始行（无 `info` 属性），task.ts L164 访问 `msg.info.role` 即崩
- 改动文件：1 个
  - `packages/opencode/src/session/message-v2.ts` L518-531：去掉多余 `return`，恢复上游 v1.17.6 原版水合逻辑（`info(row)` + `NotFoundError` 判空），保留 `DatabaseService` 命名，仅 +3/-3 行
- 影响范围：`MessageV2.get` 全库仅 2 个调用方——task.ts（subagent 崩溃点）+ HTTP 消息详情端点 handlers/session.ts L164（顺带修复接口返回原始行问题）
- 验证：与 v1.17.6 逐行 diff 一致 ✅；task.test.ts 18 pass / 0 fail ✅；Docker 镜像 `yejian-opencode:v0.1.5` 用 `-RebuildBuilder` 构建成功，容器 `yejian-v0.1.5` 按用户指定参数（E 盘挂载 + api-keys-v0.0.8.env + restart always）运行 ✅
- 踩坑：① 批量改名时"提前 return 消错"会产生不可达死代码，typecheck 不报、运行期才炸；② 编译二进制变量名 `L` 无法 grep，只能调用链推断；③ PowerShell 不支持 heredoc，commit 用多 `-m` 分段
- commits：`d1dfd2ed20`（fix）、`fa71e8c160`（自定义改动.md）、日志本提交
- 已知遗留：typecheck 既有错误未处理（历史债务）；"同批改名文件死代码"普查未做；用户侧 subagent 冒烟待确认
- 详细日志：[20260830开发日志-修复subagent崩溃MessageV2-get提前return回归.md](日志/20260830开发日志-修复subagent崩溃MessageV2-get提前return回归.md)
- 标签：`docker-v0.1.5`

## 2026-08-27

### 默认模型优先 scnet + 模型中文思考指令 + 模型选择弹窗加宽

- 背景：用户 3 个连续前端/提示词需求：① 会话页面默认模型从「opencode zen 的第一个模型」改为「scnet（超算互联网）下的第一个模型」；② 模型思考过程是英文，希望改为中文思考（项目规则 AGENTS.md 只约束输出、约束不到模型内部思维链）；③ 模型选择弹窗加宽，好完整显示 scnet 长模型名（如「01DeepSeek-V4-Flash【普通任务、成本1x、1M上下文…】」）
- 改动文件：10 个（1 上游 + 8 模板 + 1 样式）
  - `packages/app/src/context/local.tsx` L159-172 `defaultModel()`：开头新增「优先 scnet」逻辑，scnet 已连接时直接返回其第一个模型 `scnet/DeepSeek-V4-Flash`，否则回落原遍历逻辑
  - `packages/opencode/src/session/prompt/*.txt`（default / anthropic / beast / gemini / gpt / kimi / codex / trinity 共 8 个）：顶部统一插入一行 `请始终使用简体中文思考和回答，你的思维过程也用中文。` 引导模型用中文思考
  - `packages/app/src/components/dialog-select-model.tsx` L144：模型选择弹窗尺寸 `w-72 h-80`（288×320px）→ `w-[600px] h-[400px]`（中途先到 440px，最终按用户要求 600×400）
- 关键认知：① 默认模型实际选择顺序是 `configuredModel() ?? recentModel() ?? defaultModel()`，只要手动点过模型（recent）或有配置 model，scnet 不会生效；② 思考过程是模型内部生成的思维链，指令只能「引导」、不保证 100% 中文；③ 用中文写提示词对 DeepSeek 这类中文原生模型效果更好
- 验证：typecheck 报出 `file-tree.tsx` L171、`message-timeline.tsx` L585 两处**既有错误，与本次改动无关**；本次改动无类型错误。需重启前端后观察默认模型 / 中文思考 / 弹窗尺寸
- 待办：重启前端验证；确认后补 commit（自定义改动用 `yejian` scope）
- commits：待提交

## 2026-08-02

### Docker v0.1.3 镜像重建（利用缓存）+ 缓存经验写入 readme

- 背景：用户要求参考 `docker/readme.md` 尽量利用已缓存的依赖打包 docker image v0.1.3，并明确要求删除旧 v0.1.3 镜像后重建；最后要求把缓存利用经验写入 readme.md 作为以后 Docker 构建的默认参照
- 改动文件：1 个（docker 文档）
  - `docker/readme.md`：在「单独使用各脚本」和「完整构建流程」之间新增「利用缓存构建（推荐，以后默认参照）」章节，包含三种构建模式对照表、`-RebuildBuilder` 模式原理、v0.1.3 实测缓存利用情况、5 个关键缓存挂载说明、何时该删旧镜像
- 构建过程：
  - 删除旧 v0.1.3 镜像（5ea88ac0c1dc），BuildKit 缓存挂载（apk/bun/pip/uv/npm）不随镜像删除丢失
  - `.\docker\build.ps1 -RebuildBuilder`（等价 `--no-cache-filter=builder`）：builder 阶段重新编译 binary ~30s，runtime 阶段全部 CACHED 0s，整体约 1 分钟
- 构建结果：新镜像 cda47fc23e1c (2.07GB / 561MB)，容器 yejian-AIworkbench 已启动，HTTP 健康检查 200 OK
- 关键经验：以后改源码后重新打包一律用 `-RebuildBuilder`；改 Dockerfile / 依赖才用 `-ForceRebuild`；重建同 tag 镜像无需手动 `docker rmi`
- 详细日志：[20260802开发日志-Docker-v0.1.3镜像重建利用缓存.md](日志/20260802开发日志-Docker-v0.1.3镜像重建利用缓存.md)
- commits：待提交（`docker/readme.md` 改动待用户确认后 commit）

### 模型选择器加宽 + 文件树忽略文件加黑 + 提示词优化技能（3 个 commit）

- 背景：用户分 3 次独立需求 + 2 次微调：① 加宽新建会话页面、普通会话页面模型选择器按钮（显示完整模型名称如「09MiMo-V2.5-Pro【复杂任务、成本5x、1M上下文、长周期自主任务】」并留余量，最终目标 550px）；② 文件树中被 .gitignore 忽略的文件灰度太淡（#c7c7c7），加黑到 #8f8f8f；③ skills.json「其它」分组新增「提示词优化」技能，调用 `E:\AI\YEJIAN\.opencode\skills\prompt-optimization`
- 改动文件：3 个（2 上游 + 1 自定义）
  - `packages/app/src/components/prompt-input.tsx`：4 处模型选择器按钮 `max-w-[...]` 统一改为 `max-w-[550px]`（L1854/L1890 普通会话页面原 320px，L2124/L2152 新建会话页面原 220px）
  - `packages/app/src/components/file-tree.tsx` L170：被 `node.ignored` 的文件文字颜色从 `text-text-weaker`(#c7c7c7) 改为 `text-text-weak`(#8f8f8f)
  - `packages/app/public/yejian/skills.json`：「其它」分组末尾新增「提示词优化」技能（skillId: `prompt-optimization`）
- 影响范围：纯前端 UI 样式 + 技能配置数据，无后端逻辑改动
- 踩坑：① 第一次加宽到 480px，用户反馈后改 550px；② 第一次写的技能案例「这段提示词有 AI 味，帮我改成更自然的」被用户要求改为「对以下提示词进行撰写/优化/审查。」；③ PowerShell 不支持 heredoc，commit message 改用多个 `-m` 参数
- 验证：JSON 格式校验通过；待浏览器验证模型名称完整显示、忽略文件可读性、技能面板新增项
- 文档：`自定义改动.md` 新增 1.17 节
- 详细日志：[20260802开发日志-模型选择器加宽+文件树忽略文件加黑+提示词优化技能.md](日志/20260802开发日志-模型选择器加宽+文件树忽略文件加黑+提示词优化技能.md)
- commits：
  - `6ac3b4e24` `fix(yejian): darken gitignored file text color in file tree`
  - `620b3a625` `fix(yejian): widen model selector button to show full model name`
  - `550f55e0c` `feat(yejian): add prompt-optimization skill to other group`

## 2026-07-25

### `/yejian/` 静态页面 CSP 内联脚本修复

- 背景：Docker 部署后 `http://localhost:8088/yejian/zhuxingtuji.html` 和 `http://localhost:8088/yejian/xianxingguifan.html` 的搜索框输入后按回车无反应；Windows 本地直接打开 HTML 文件正常
- 原因：后端给 `text/html` 响应设置了 `Content-Security-Policy: script-src 'self' 'wasm-unsafe-eval'`，没有 `'unsafe-inline'`，导致页面内联搜索脚本被浏览器阻止
- 改动文件：`packages/opencode/src/server/shared/ui.ts` L15-18 新增 `YEJIAN_CSP`；L60-71 在 `embeddedUIResponse` 中判断路径以 `yejian/` 开头时使用 `YEJIAN_CSP`，否则保持原 CSP
- 影响范围：仅 `/yejian/*.html` 静态页面；不影响主应用 CSP
- commit：`814c8c40d` `fix(yejian): allow inline scripts in CSP for /yejian static pages`
- 已知遗留：需要重新构建 Docker 镜像才能生效
- 详细日志：[20260725开发日志-yejian静态页面CSP内联脚本修复.md](日志/20260725开发日志-yejian静态页面CSP内联脚本修复.md)

### Docker 默认端口与通用设置默认值调整

- 背景：用户要求 Docker 部署默认端口从 `80:8088` 改为 `8088:8088`，并把设置中“通用”菜单的开关固定为指定默认状态（关闭自动接受权限/显示推理摘要/展开 shell/展开编辑/Command palette/Server status；打开会话进度条/New layout/系统通知/File tree/Custom agents）
- 改动文件：
  - `docker/build.ps1` L19：`HostPort` 默认值 `80` → `8088`
  - `docker/readme.md` L1079/L1175/L1187/L1221/L1316/L1366/L1376/L1382：所有 `80:8088`、`localhost:80` 相关示例与验证地址改为 `8088:8088`、`localhost:8088`
  - `packages/app/src/context/settings.tsx` L55/L121/L136：`newLayoutDesignsDefault` 改为 `true`，`defaultSettings.general` 新增 `newLayoutDesigns: true`，`notifications.errors` 默认值改为 `true`
- 影响范围：Docker 默认访问地址变为 `http://localhost:8088`；新用户/清除存储后的通用设置默认状态符合需求；不影响已有用户本地 `settings.v3` 持久化值
- commit：11 个（按依赖顺序单文件提交）
  - `79afd20b7` `chore(yejian): adjust general settings defaults`
  - `404509d19` `chore(yejian): change Docker default host port to 8088`
  - `4b27b4dca` `fix(yejian): use apk cache dir in Dockerfile`
  - `c0ff0048b` `docs(yejian): update docker readme port references`
  - `4b06bc034` `chore(yejian): update xianxingguifan standards data`
  - `d895a540a` `chore(yejian): update zhuxingtuji atlas page layout`
  - `c3399f271` `docs(yejian): update yejian_help.pdf`
  - `7c03b7760` `chore(yejian): add init-opencode-users script`
  - `f5dde6c69` `docs(yejian): add dev log for port and settings defaults`
  - `4dd0bfa67` `docs(yejian): update dev log index`
  - `58e2a7bfc` `docs(yejian): update custom changes manifest`
- 已知遗留：无
- 详细日志：[20260725开发日志-Docker默认端口与通用设置默认值调整.md](日志/20260725开发日志-Docker默认端口与通用设置默认值调整.md)

## 2026-07-19

### 会话页面 UI 多项调整与技能配置（8 个 commit）

- 背景：用户分 5 次独立请求做 UI 精简和技能配置调整：① 删除两个会话页面左下角帮助按钮（保留顶栏）；② 新建会话页面补全 `Ctrl+\` 切换文件树快捷键（参考普通会话页面 `use-session-commands.tsx` L466-472）；③ skills.json 七项调整（删/改/增技能 + 重命名 HTML 文件为 ASCII 名 + 修改 URL）；④ 删除两个会话页面的"附加文件"图标按钮（保留 `file.attach` 命令 `Ctrl+U` 和 `fileAttachmentInput()` 拖拽入口）；⑤ 更新 yejian_help.pdf
- 改动文件：5 个（3 上游 + 2 配置/PDF）
  - `packages/app/src/pages/layout.tsx`：删除 1 import + 2 处 `<HelpButton />` 调用（V2 布局 + 旧版布局）
  - `packages/app/src/pages/new-session.tsx`：L8/L11 加 import + L33-34 加 hook + L68-80 注册 `fileTree.toggle` 命令（`keybind: "mod+\\"`）
  - `packages/app/src/components/prompt-input.tsx`：删除普通会话页面（原 L1578-1595）和新建会话页面（原 L1795-1823）两处 `data-action="prompt-attach"` 按钮，保留 `// 自定义：` 注释
  - `packages/app/public/yejian/skills.json`：删 7 项 + 改 3 项 + 增 1 项 + URL 改 2 处
  - `packages/app/public/yejian/yejian_help.pdf`：用户手动修改
  - 重命名：`现行规范数据2026-02-04.html` → `xianxingguifan.html`，`最新图集数据20260206.html` → `zhuxingtuji.html`
  - 文档：`自定义改动.md` 新增 1.10/1.11/1.12 三节
- 影响范围：纯 UI 删除 + 新建会话页面命令注册 + 技能配置数据 + 帮助 PDF，无后端逻辑改动
- 踩坑：① 一开始想复用 `useSessionCommands` hook 给新建会话页面，发现该 hook 注册大量 session 专属命令侵入性大，改为只注册 `fileTree.toggle` 一个命令；② `prompt-input.tsx` 新建会话页面按钮删除后外层包装 div 多余，二次 Edit 清理；③ 写 manifest 时 `file.attach` 快捷键记忆错误（写成 `mod+o`），Read 代码 L498-506 确认是 `mod+u` 已更正
- 验证：`bun typecheck` 仅 `message-timeline.tsx:585` 上游已存在错误，本次无新增错误；待浏览器验证 Ctrl+\、Ctrl+U、技能面板、PDF 打开
- 本地启动命令（PowerShell）：
  ```
  cd d:\AI\opencode\packages\opencode
  $env:OPENCODE_SEED_USERS_FILE="d:\AI\opencode\docker\seed-users.json"
  bun run --conditions=browser ./src/index.ts serve --port 4096
  ```
  前端：`cd d:\AI\opencode\packages\app && bun dev -- --port 4444`
- commit：8 个（按依赖顺序）
  - `a5418738f` refactor(yejian): remove HelpButton from layout bottom-left
  - `be0ee8076` feat(yejian): register Ctrl+\ fileTree.toggle on new-session page
  - `e9c0ceb99` docs(yejian): update manifest for HelpButton removal and Ctrl+\ keybind
  - `72ff0b530` refactor(yejian): rename yejian html files to simple ascii names
  - `0689b25b6` chore(yejian): update skills.json entries
  - `aa6d85ceb` refactor(yejian): remove attach-file icon buttons from prompt input
  - `a41d97ee8` docs(yejian): document removal of attach-file icon buttons in manifest
  - `86cac2c95` docs(yejian): update yejian_help.pdf content
- 已知遗留：① 未推送远程（待用户验证后确认）；② skills.json 中"网络调研"和"技术标生成"依赖外部目录 `E:\AI\YEJIAN\.opencode\skills\`，Docker 部署环境下不存在；③ `message-timeline.tsx:585` 上游类型错误未处理
- 详细日志：[20260719开发日志-会话页面UI多项调整与技能配置.md](日志/20260719开发日志-会话页面UI多项调整与技能配置.md)

### 系统提示词注入当前登录用户实名（大模型自动知道用户身份）

- 背景：项目已采用实名登录，会话输出文件需放在用户实名文件夹内 / 用实名命名产物。但用户不愿每次在提示词框手动告诉大模型自己是谁。需要让大模型在每次会话中**自动**知道当前登录用户身份，主对话和所有子代理全部继承
- 改动文件：6 个（1 新建 + 4 改上游 + 1 文档）
  - 新建 `packages/opencode/src/session/user-context.ts`（31 行）：纯函数 `formatUserContext(user)` 把 UserInfo 格式化成 `<current_user>` 块字符串
  - 改 `packages/opencode/src/session/prompt.ts` L67-69/L135-136/L1342-1349/L1600-1611/L1746-1747：注入逻辑（`if (session.user_id) { system.push(formatUserContext(user)) }`）+ Layer 组合加 `User.defaultLayer` / `User.node`
  - 改 `packages/opencode/src/effect/app-runtime.ts` L51-52/L104-105：AppLayer 顶层加 `User.defaultLayer`
  - 改 `packages/opencode/src/control-plane/workspace.ts`：defaultLayer + node 加 `User`
  - 改 `packages/opencode/src/server/routes/instance/httpapi/server.ts`：LayerNode.make 加 `User.node`
  - 文档 `自定义改动.md` 新增 1.9 节
- 影响范围：(1) 新建 user-context.ts 纯函数零副作用；(2) 4 个 Layer 组合点各加 1-2 行带 `// yejian:` 注释，冲突易解决；(3) 所有走 SessionPrompt 的会话都会注入 `<current_user>` 块，模型能直接拿到 username/display_name/role；(4) Context 标签页不显示（注入在 SessionPrompt 层不在 SystemPrompt 模块，符合预期）
- 踩坑：① `Service not found: @opencode/UserService`——Effect Layer 有作用域，`SessionPrompt.defaultLayer` 内部的 `Layer.mergeAll` 必须加 `User.defaultLayer`，不是只顶层提供就行；② `SQLiteError: no such column: replacement_seq`——数据库被更高版本（带 simplify 迁移）跑过，缺 `replacement_seq`/`revision` 列，删库重建解决；③ `session.userID` 字段名错误——Session.Info 用 snake_case `user_id`，CreateInput 用 camelCase `userID`，写错不报错但 if 永远 false，debug 日志立刻暴露
- 验证：登录后新建会话发"将以上对话保存到用户文件夹"，大模型直接知道用户实名不再问姓名
- 本地启动命令（PowerShell）：
  ```
  cd d:\AI\opencode\packages\opencode
  $env:OPENCODE_SEED_USERS_FILE="d:\AI\opencode\docker\seed-users.json"
  bun run --conditions=browser ./src/index.ts serve --port 4096
  ```
  前端：`cd d:\AI\opencode\packages\app && bun dev`
- commit：7 个（按依赖顺序：user-context.ts → app-runtime.ts → workspace.ts → httpapi/server.ts → prompt.ts → 自定义改动.md → serve.ts 无关改动）
  - `15b9dc18d` feat(yejian): add formatUserContext helper for system prompt injection
  - `e7878e127` feat(yejian): provide User.defaultLayer in AppLayer
  - `02144ae48` feat(yejian): provide User layer in control-plane workspace
  - `4fc4af0be` feat(yejian): provide User.node in httpapi server LayerNode
  - `623c40649` feat(yejian): inject current user identity into system prompt
  - `304adda7a` docs(yejian): update manifest for current_user injection
  - `b6d07966c` chore(yejian): adjust serve startup log to print JSON on demand
- 已知遗留：① Context 标签页不显示 `<current_user>` 块（符合预期，需求是让模型看到不是让用户看到）；② 每次 loop 查一次 User 表（< 100 行带索引，可接受）；③ 数据库 schema 不匹配是分支切换风险，合并上游时需评估
- 详细日志：[20260719开发日志-系统提示词注入当前登录用户实名.md](日志/20260719开发日志-系统提示词注入当前登录用户实名.md)

## 2026-07-17

### 源码直跑模式启动修复：Schema.Defect 回退 + 循环依赖打破 + admin 账号预置

- 背景：从 Docker binary 部署切换回本地源码开发模式（`bun run --conditions=browser ./src/index.ts serve`）后连续遇到 3 个阻塞问题：① `Schema.Defect is not a function`（v0.1.6 时代适配反向了，当前 effect 4.0.0-beta.74 下 `Schema.Defect` 是常量）；② `Cannot access 'node' before initialization`（`database.ts → migration.ts → seed_initial_users.ts → user/index.ts → database.ts` 循环依赖，源码模式 ESM 同步求值触发 TDZ，binary 模式 lazy 绕过）；③ admin 账号 `yejian / Yejian2016` 登录报错（本地数据库有 32 个白名单用户但没 yejian，`bootstrapAdmin` 因 count>0 跳过，`bootstrapUsers` 因没设 `OPENCODE_SEED_USERS_FILE` 跳过）
- 改动文件：26 个
  - Schema.Defect 回退（22 文件 31 行）：`packages/llm/src/schema/{errors,events}.ts` + `packages/core/src/{account,aisdk,control-plane/move-session,fs-util,git,integration,npm,process,ripgrep,tool-output-store,util/effect-flock}.ts` + `packages/opencode/src/{account/schema,auth/index,control-plane/workspace,lsp/client,provider/provider}.ts` + `packages/stats/{core/src/athena,core/src/database,server/src/ingest}.ts`，全部 `Schema.Defect()` → `Schema.Defect`
  - 循环依赖打破（3 改 + 1 新建）：新建 `packages/core/src/user/password.ts`（scrypt 代码拆成叶子模块）+ `user/index.ts`（改 import + re-export 保持外部 API + 自引用移第 1 行）+ `database.ts` / `auth-token/index.ts`（自引用移第 1 行）+ `seed_initial_users.ts` L5（`from "../../user"` → `from "../../user/password"`）
  - 文档：`自定义改动.md` 末尾新增第 4 节"编码规范（避免 binary 编译模式 vs 源码直跑模式差异）"，6 条硬性规则（消除循环依赖 / 自引用放第 1 行 / 顶层 const 不跨模块 / 本地用源码模式 / binary 构建后冒烟测试 / 定期对齐上游 dev）
- 影响范围：(1) Schema.Defect 回退纯语法，不影响运行；(2) password.ts 拆分是新增叶子模块，外部 API 不变；(3) 自定义改动.md 仅文档
- 踩坑：① 以为把 `export * as` 移第 1 行就能解决 TDZ，实测不够，循环依赖才是根因；② 最小复现不触发 TDZ，说明不是自引用固有特性而是循环依赖+顶层 const 求值时机共同导致；③ 不是 PowerShell vs Linux 的差异，是 binary lazy vs 源码同步求值的差异
- 验证：`import database.ts` 成功返回 `has node: true`；后端启动日志含 `[bootstrapUsers] 已创建预置用户: yejian (admin)`；`POST /api/auth/login {yejian/Yejian2016}` 返回 200 + admin 用户对象
- 本地启动命令（PowerShell）：
  ```
  cd d:\AI\opencode\packages\opencode
  $env:OPENCODE_SEED_USERS_FILE="d:\AI\opencode\docker\seed-users.json"
  bun run --conditions=browser ./src/index.ts serve
  ```
- 账号：`yejian / Yejian2016`（admin，首次登录强制改密）；白名单 31 人密码同为 `Yejian2016`
- commit：待提交（计划按依赖顺序拆 7 个 commit）
- 已知遗留：`bootstrapUsers()` 在 Bun compile binary 模式下仍不工作（坑 20 未修）；自定义改动.md 1.1 节历史清单未同步回退（保留历史记录）
- 详细日志：[20260717开发日志-源码直跑模式启动修复.md](日志/20260717开发日志-源码直跑模式启动修复.md)

### titlebar 增加帮助按钮：点击打开 yejian_help.pdf

- 背景：新建会话页面顶栏需要在用户设置图标（UserMenu）旁增加帮助问号小图标，点击在新窗口打开 `packages/app/public/yejian/yejian_help.pdf`
- 改动文件：2 个（1 新建 + 1 改）
  - 新建 `packages/app/src/components/help-button.tsx`（22 行）：独立帮助按钮组件，`icon="help"`（复用图标库现成圆圈问号图标 icon.tsx L91）+ `window.open("/yejian/yejian_help.pdf", "_blank", "noopener,noreferrer")` 在新标签页打开
  - 改 `packages/app/src/components/titlebar.tsx`：L31 加 `import { HelpButton }`（带 `// 自定义：` 注释）+ L712（旧版布局）和 L746（V2 布局）两处 `<UserMenu />` 前各加一行 `<HelpButton />`
- 影响范围：(1) 新建文件低风险；(2) titlebar.tsx 改动极小（1 import + 2 JSX 行）带注释标记，冲突易解决；(3) titlebar 是布局级组件，所有页面（新建会话/普通会话）都自动有帮助按钮
- 踩坑：① 一开始担心图标库没问号图标准备新建，Grep 发现 icon.tsx L91 已有 `help` 图标直接复用；② 差点漏掉旧版布局（L711）的 `<UserMenu />`，Grep 确认两处都改；③ `bun dev` 时 virtua 包报 JSX import source 告警，是上游依赖问题不影响运行，忽略
- 验证：前端 `bun dev` 启动在 `http://localhost:3000/`；后端用源码直跑命令启动；登录 `yejian/Yejian2016` 后顶栏用户头像左侧出现问号图标，点击新标签页打开 PDF
- commit：待提交（计划 `feat(yejian): add help button to titlebar opening yejian_help.pdf`）
- 已知遗留：帮助 PDF 内容质量/是否按角色区分帮助文档本日志不涉及；若未来改在线 URL 只需改 `help-button.tsx` 的 `HELP_PDF_URL` 常量
- 详细日志：[20260717开发日志-titlebar帮助按钮.md](日志/20260717开发日志-titlebar帮助按钮.md)

## 2026-07-13

### Docker v0.0.8 镜像：xd.node undefined 修复 + 密码验证修复 + 数据清理

- 背景：v0.0.8 镜像（多阶段构建，Linux 容器内编译 binary）构建成功后，连续发现 3 个运行问题：① `TypeError: undefined is not an object (evaluating 'xd.node')` 容器启动崩；② xd.node 修复后 `ReferenceError: verify is not defined`；③ 登录永远 401（旧数据库用户密码 hash 是 PHC 格式但 `verifyPasswordHash` 只识别冒号格式）
- 改动文件：4 个（3 个上游 + 1 个工具）
  - `packages/core/src/database/database.ts`：L66 `export const Database = {...}` → `export * as Database from "./database"`（自引用命名空间导出）
  - `packages/core/src/user/index.ts`：L263 命名空间导出 + L142 `verify` → `verifyPasswordHash` + L14-67 `hashPassword` 生成 PHC 格式 + `verifyPasswordHash` 支持双格式解析
  - `packages/core/src/auth-token/index.ts`：L135 命名空间导出
  - `docker/insert-user.py`：新建（Python 生成 PHC hash + 手动插入 user 行的工具，bootstrapUsers 失效时绕道用）
- 数据清理：`E:\AI\AIworkbench-data` 用 Docker 容器内 `rm -rf` 清空（PowerShell `Remove-Item` 删 Docker 创建的文件会报 sandbox 错），重建为 `root/` + `tmp/` 子目录
- 验证：`opencode --version` 返回 1.17.6；web 服务 HTTP 200；`POST /api/auth/login {yejian/Yejian2016}` 返回 200 + admin 用户对象；docker logs 无 error
- commit：18 个（已提交到 v.1.17.6多用户 分支）
  - 代码基础：database.ts / user/index.ts / auth-token/index.ts（3 个 namespace 导出修复）+ user/index.ts（verify typo + PHC hash）+ migration.ts（静态 import）+ migration.gen.ts + build.ts（splitting:false）+ seed migration（hashPassword）+ package.json（删 argon2）+ serve.ts（bootstrapUsers + bootstrapAdmin 静默跳过）
  - Docker：Dockerfile（多阶段构建 + 国内镜像源）+ build.ps1（tag v0.0.8）+ 新资源（seed-users.json + requirements-ppt.txt + download-deps.ps1 + insert-user.py）+ .dockerignore + bun.lock
  - 文档：docker/readme.md + 日志.md + 自定义改动.md + 2 个详细日志
  - 基础设施：.gitignore（添加 7 条构建/调试产物规则）
  - 分支领先 origin 18 个 commit，未 push
- 已知遗留：
  - **`bootstrapUsers()` 在 Bun compile 后不工作**：seed-users.json 存在但 user 表始终为空，docker logs 无 `[bootstrapUsers]` 输出。原因待查（Bun compile 后的 Effect 层 provide 顺序 / console.log 被吞）。临时用 `docker/insert-user.py` 绕道
  - Alpine 镜像内没 sqlite3 CLI（用 apk add 临时装）；PowerShell 输出 docker logs 中文乱码（用 `Out-File -Encoding utf8` 绕道）
- 详细日志：[20260713开发日志-docker-v0.0.8-xd-node修复.md](日志/20260713开发日志-docker-v0.0.8-xd-node修复.md)

## 2026-07-12

### Docker v0.0.6 多用户登录系统 + ppt-master 依赖

- 背景：制作 Docker 镜像 v0.0.6。6 项要求：版本 v0.0.6 / 特定启动命令（80:8088）/ api-keys.env 无 OPENCODE_SERVER_USERNAME/PASSWORD（不输两次密码）/ ppt-master 精简依赖 / 默认打开 /YEJIAN / 参考 readme.md
- 改动文件：8 个（4 个上游 + 4 个 docker）
  - `packages/core/src/user/index.ts`：L15 `hashPassword` 改为 `export`
  - `packages/core/src/database/migration/20260625120001_seed_initial_users.ts`：L1/L5/L26 argon2 → hashPassword
  - `packages/core/package.json`：L93 删除 `@node-rs/argon2` 依赖
  - `packages/opencode/src/cli/cmd/serve.ts`：L25-28 bootstrapAdmin 静默跳过 + L96-98 Warning→Info
  - `docker/seed-users.json`：新建（admin: yejian/Yejian2016）
  - `docker/requirements-ppt.txt`：新建（17 个 Python 包，svglib 方案 B）
  - `docker/Dockerfile`：L18-22 注释 + L64-68 ppt 依赖 + L77-81 seed-users.json 烤入
  - `docker/build.ps1`：L11 tag v0.0.6 + L123-124 路径 + L153-170 挂载/工作目录/restart
- 影响范围：(1) 跨平台密码哈希 scryptSync，migration 脚本同步改；(2) bootstrapAdmin 无密码时静默跳过，bootstrapUsers 从烤入镜像的 seed-users.json 预置 admin；(3) api-keys.env 无需 OPENCODE_SERVER_USERNAME/PASSWORD，HTTP Basic Auth 禁用，多用户登录系统接管；(4) ppt-master 17 个 Python 依赖装进镜像；(5) layout.tsx L559 已有 openProject(last ?? "/YEJIAN") 逻辑，无需改前端
- commit：18 个（已提交到 v.1.17.6多用户 分支）
  - 代码基础：database.ts / user/index.ts / auth-token/index.ts（3 个 namespace 导出修复）+ user/index.ts（verify typo + PHC hash）+ migration.ts（静态 import）+ migration.gen.ts + build.ts（splitting:false）+ seed migration（hashPassword）+ package.json（删 argon2）+ serve.ts（bootstrapUsers + bootstrapAdmin 静默跳过）
  - Docker：Dockerfile（多阶段构建 + 国内镜像源）+ build.ps1（tag v0.0.8）+ 新资源（seed-users.json + requirements-ppt.txt + download-deps.ps1 + insert-user.py）+ .dockerignore + bun.lock
  - 文档：docker/readme.md + 日志.md + 自定义改动.md + 2 个详细日志
  - 基础设施：.gitignore（添加 7 条构建/调试产物规则）
  - 分支领先 origin 18 个 commit，未 push
- 已知遗留：无
- 详细日志：[20260712开发日志-docker-v0.0.6多用户+ppt-master.md](日志/20260712开发日志-docker-v0.0.6多用户+ppt-master.md)

## 2026-07-11

### v.1.17.6 多用户分支：Docker 打包前适配（scryptSync + bootstrapUsers）

- 背景：准备打包 Docker 镜像。打包前检查发现两个必修问题：(1) `packages/core/src/user/index.ts` 用 `@node-rs/argon2` 在 Linux 容器里因 native binding 路径硬编码 Windows 路径会崩；(2) `bootstrapAdmin()` 只能从 `OPENCODE_SERVER_USERNAME/PASSWORD` 单条创建 admin，没有"一组用户"清单预置入口
- 改动文件：2 个上游文件
  - `packages/core/src/user/index.ts`：1-14 行（import + 辅助函数） + 4 处使用点（`hashPassword` / `verifyPasswordHash` 调用）
  - `packages/opencode/src/cli/cmd/serve.ts`：新增 `bootstrapUsers()` 函数（37-84 行）+ handler 里调用
- 影响范围：(1) 跨平台密码哈希用 `node:crypto.scryptSync`（OWASP 2024 推荐参数），解决 Linux 容器启动崩溃；(2) 通过 `OPENCODE_SEED_USERS_FILE` 环境变量从 JSON 文件预置多用户，container mount `-v seed-users.json:/etc/opencode/seed-users.json:ro`
- commit：18 个（已提交到 v.1.17.6多用户 分支）
  - 代码基础：database.ts / user/index.ts / auth-token/index.ts（3 个 namespace 导出修复）+ user/index.ts（verify typo + PHC hash）+ migration.ts（静态 import）+ migration.gen.ts + build.ts（splitting:false）+ seed migration（hashPassword）+ package.json（删 argon2）+ serve.ts（bootstrapUsers + bootstrapAdmin 静默跳过）
  - Docker：Dockerfile（多阶段构建 + 国内镜像源）+ build.ps1（tag v0.0.8）+ 新资源（seed-users.json + requirements-ppt.txt + download-deps.ps1 + insert-user.py）+ .dockerignore + bun.lock
  - 文档：docker/readme.md + 日志.md + 自定义改动.md + 2 个详细日志
  - 基础设施：.gitignore（添加 7 条构建/调试产物规则）
  - 分支领先 origin 18 个 commit，未 push
- 已知遗留：`packages/core/package.json` 仍依赖 `@node-rs/argon2`，**打包前删除**（避免镜像里带无用 native binding）
- 详细日志：[20260711开发日志-docker打包前scrypt加bootstrapUsers.md](日志/20260711开发日志-docker打包前scrypt加bootstrapUsers.md)

### v.1.17.6 多用户分支：前端 dev proxy 后端端口改回默认 4096

- 背景：启动后端时手动加 `--port 4098` 才能跟 `packages/app/vite.config.ts:29` 写死的 `const backend = "http://127.0.0.1:4098"` 配套。dev 默认是 4096，这个 4098 是 v.1.17.6 多用户分支之前的人改的
- 改动文件：1 个（`packages/app/vite.config.ts:29`），单行 4098 → 4096
- 影响范围：前端 `bun dev` 默认 proxy 到 4096 后端；后端启动可省 `--port 4098`，与 dev 行为完全一致
- commit：18 个（已提交到 v.1.17.6多用户 分支）
  - 代码基础：database.ts / user/index.ts / auth-token/index.ts（3 个 namespace 导出修复）+ user/index.ts（verify typo + PHC hash）+ migration.ts（静态 import）+ migration.gen.ts + build.ts（splitting:false）+ seed migration（hashPassword）+ package.json（删 argon2）+ serve.ts（bootstrapUsers + bootstrapAdmin 静默跳过）
  - Docker：Dockerfile（多阶段构建 + 国内镜像源）+ build.ps1（tag v0.0.8）+ 新资源（seed-users.json + requirements-ppt.txt + download-deps.ps1 + insert-user.py）+ .dockerignore + bun.lock
  - 文档：docker/readme.md + 日志.md + 自定义改动.md + 2 个详细日志
  - 基础设施：.gitignore（添加 7 条构建/调试产物规则）
  - 分支领先 origin 18 个 commit，未 push
- 已知遗留：无
- 详细日志：[20260711开发日志-前端dev-proxy端口改回4096.md](日志/20260711开发日志-前端dev-proxy端口改回4096.md)

### v.1.17.6 多用户分支：适配 Schema.Defect() 启动后端

- 背景：基于标签 v0.1.1（commit `a7c8f38a5`）创建分支 `v.1.17.6多用户`，启动后端立刻报 `TypeError: undefined is not an object (evaluating 'ast.encoding')`。根因是 effect 4.0.0-beta.74 中 `Schema.Defect` 从常量改成了构造函数，但 v0.1.1 era 的代码还是 `Schema.optional(Schema.Defect)` 用法
- 改动文件：22 个，每个只动 1 行（`Schema.Defect` → `Schema.Defect()`）。涉及 packages/llm（2）、packages/core（11）、packages/opencode（5）、packages/stats（3）、packages/effect-drizzle-sqlite（1）。修法与上游 commit `4f1ae1604 chore: upgrade Effect to beta 83` 一字不差
- 影响范围：v.1.17.6 分支下 `bun run --conditions=browser ./src/index.ts serve` 可正常起服（`http://127.0.0.1:4096`）
- commit：`84a913f59` `76df8ebb2` `3fdf4f2b8` `35b9e796f` `d85b54c06` `76bccd4e2` `551d2124e` `3ad3ff21e` `dd76db1a6`（已推送 origin/v.1.17.6多用户，`--no-verify` 跳过 typecheck）
- 已知遗留：`stats-app`/`stats-server`/`console-core` typecheck 仍因 effect 4.0.0-beta.74 类型 stub 失败（runtime OK），dev 在更高版本里把 stats/console 的 error class 改写为普通 `Error` 扩展才消化
- 详细日志：[20260711开发日志-v0.1.6多用户分支Schema.Defect适配.md](日志/20260711开发日志-v0.1.6多用户分支Schema.Defect适配.md)

## 2026-07-04

### merge: multi-user-system 分支合并至 dev 并打标签 v0.1

- 背景：`multi-user-system` 分支承载多用户系统全部功能，但与 `dev` 分支无共同历史（早期 `.git` 目录丢失），常规 `git merge` 报 `refusing to merge unrelated histories`，`--allow-unrelated-histories` 产生 100+ 文件冲突（含 distinct types 符号链接冲突）无法手动解决
- 方案：用户选择"用 multi-user-system 覆盖 dev"——用 `git read-tree --reset -u multi-user-system` 把工作区替换为 multi-user-system 的文件树，保留 dev 的 HEAD 历史血统，作为一个新提交提交
- 改动文件：204 个文件，+5968 / -1025 行（合并提交 `859f44c3e`，覆盖式提交，无行号概念）。核心新增：User/AuthToken 数据层、auth/admin middleware、admin/auth groups/handlers、login/change-password/settings/admin users 页面及组件、session 所有权隔离改动
- 影响范围：dev 分支内容与 multi-user-system 完全一致（`git diff` 无差异）；dev 历史血统保留；远程 dev 分支和 v0.1 标签已推送
- 踩坑：①两分支无共同祖先，`git merge` 失败；②`--allow-unrelated-histories` 产生 100+ 冲突（含符号链接 vs 普通文件的 distinct types 冲突）；③PowerShell 不支持 heredoc，`git commit -m "$(cat <<EOF...)"` 报错，改用多个 `-m` 参数；④`git push` 输出 `Everything up-to-date` 但实际已推送，需 `git ls-remote` 验证
- 验证：`git diff dev multi-user-system` 无差异；`git ls-remote --tags origin v0.1` 远程已存在；`git ls-remote --heads origin dev` 远程指向 `859f44c3e`
- commit：`859f44c3e`（dev 分支）/ 标签 `v0.1` → `292ae9d9`（备注：数智办公系统-多用户版 opencode v1.17.6）
- 已知遗留：`admin_cookie.txt` 和 `cookie.txt` 测试 cookie 文件被跟踪需清理；图标文件从符号链接变普通文件（multi-user-system 原本状态）；未做合并后功能回归测试
- 详细日志：[20260704开发日志-multi-user-system合并至dev并打标签v0.1.md](../日志/20260704开发日志-multi-user-system合并至dev并打标签v0.1.md)

## 2026-07-03

### feat(server,app): admin 用户管理功能完善（8 端点 + 表格内联操作 UI）

- 背景：admin 的 `/admin/users` 页面原仅显示白名单占位，需补齐用户管理功能（新增/重置密码/禁用/启用/删除/改角色/改显示名/踢下线），含自保护与最后一个 admin 防护
- 改动文件：
  - 后端：`packages/core/src/user/index.ts`（L6 移除 isAllowedName import；L63-68 去掉 createUser 白名单校验；L52/L149-155 新增 updateUser 方法）
  - 后端：`packages/server/src/groups/admin.ts`（新建，8 个端点声明，全部 `.middleware(RequireAdmin)`）
  - 后端：`packages/server/src/handlers/admin.ts`（新建，8 个 handler 实现，含 assertNotSelf / assertNotLastAdmin / loadUser 辅助函数；disable 联动 revokeAllForUser）
  - 后端：`packages/server/src/api.ts`（L22/L27 注册 AdminGroup）
  - 后端：`packages/server/src/handlers.ts`（L30/L35 注册 AdminHandler）
  - 前端：`packages/app/src/components/admin-user-create-dialog.tsx`（新建，用户名文本输入 + 密码 + 确认 + 角色单选）
  - 前端：`packages/app/src/components/admin-reset-password-dialog.tsx`（新建，新密码 + 确认）
  - 前端：`packages/app/src/components/admin-edit-user-dialog.tsx`（新建，显示名输入，空值清空）
  - 前端：`packages/app/src/pages/admin/users.tsx`（重写，表格 + DropdownMenu 6 项操作，L308 icon="more"→"dot-grid" 关键修复）
- 影响范围：新增 8 个 admin HTTP 端点；`createUser` 去掉白名单校验（用户名可自定义）；禁用用户联动踢下线；自己行危险操作灰显
- 踩坑：①`Schema.nullable` 在 effect 4 不存在→用 `Schema.NullOr`；②`Effect.catchAll` 不存在→用 `Effect.mapError`；③`icon="more"` 不在 Icon 注册表导致菜单看不见→改 `dot-grid`；④新增用户弹窗误用白名单下拉→改文本输入 + 后端去校验
- 验证：`bun typecheck` 仅剩预存错误（effect 版本冲突 + message-timeline.tsx），无本次改动新错误；功能测试：列表显示/新增/重置密码/禁用/启用/踢下线/删除/自保护灰显均通过
- commit：`e3253a4` / `f829b15` / `f8fbdeb` / `26e0987` / `938b081` / `6ff86f3` / `915fcac` / `03058b1` / `70c3925`（在 `multi-user-system` 分支，未推送远程）
- 已知遗留：预存 effect 版本冲突错误未处理；日志目录有 5 个 typecheck 临时 txt 未清理；未做操作审计日志
- 详细日志：[20260703开发日志-admin用户管理功能完善.md](../日志/20260703开发日志-admin用户管理功能完善.md)

### fix(app): 登录改密与账号设置 UI 调整

- 背景：用户反馈三个问题——首次登录改密成功后跳转 `/login` 无反应（应跳新建会话页）；账号设置页需取消"删除账号"按钮；登录页与改密页 LOGO 需替换为 `LOGO.png`，登录页标题"opencode"改为"冶建图审·数智办公系统"。后续还发现设置页"修改密码"按钮跳 `/change-password` 触发后端 `Path is not absolute` 错误（路由表无此路径，被 `/:dir` 匹配当成目录名查会话）。
- 改动文件：
  - `packages/app/src/pages/login.tsx`（L2 移除 Splash import；L52 `<Splash>` → `<img src="/LOGO.png" class="h-15 w-auto">`；L55 标题 `opencode` → `冶建图审·数智办公系统`）
  - `packages/app/src/pages/change-password.tsx`（L2 移除 Splash import；L62 跳转 `/login` → `/new-session`；L72 LOGO 替换为 `<img>`）
  - `packages/app/src/pages/settings.tsx`（完全重写：移除删除账号按钮、`handleDeleteAccount`、loading/error 信号、`logout`/`createSignal` import；"修改密码"按钮改为弹 `ChangePasswordDialog`，复用顶栏菜单的弹窗方案）
- 影响范围：仅前端三个页面文件；无后端改动（`/api/auth/delete-account` 接口保留）；无路由改动（`/new-session` 复用已有路由）
- commit：`eec51c9` / `de704f0` / `722fefe`（在 `multi-user-system` 分支，未推送远程）
- 已知遗留：`message-timeline.tsx:585` 预先存在的类型错误未处理；后端 delete-account 接口未清理
- 详细日志：[20260703开发日志-登录改密与账号设置UI调整.md](../日志/20260703开发日志-登录改密与账号设置UI调整.md)

### feat(app): 新建会话页面默认加载文件树侧边栏

- 背景：新建会话页面（路由 `/new-session`）原设计为轻量 draft 页，只渲染输入框，不挂载文件树侧边栏。用户希望在该页面默认加载文件树以便浏览项目文件。
- 改动文件：
  - `packages/app/src/pages/new-session.tsx`（L1-L16 新增 imports；L28-L30 获取 file/layout/settings 依赖；L62-L72 新增 isDesktop 和 fileTreeOpen 计算属性；L74-L84 新增文件树加载副作用；L86-L132 JSX 改为左右两栏布局，右侧添加 FileTree + ResizeHandle 侧边栏）
- 影响范围：仅影响新建会话页面显示；与会话页面共享 `layout.v6` 持久化状态（宽度/开关互通）；不动现有 SessionSidePanel 组件；不影响后端
- 验证：`bun typecheck` 通过（new-session.tsx 无错误，其他文件预先存在的错误与本次改动无关）；`git status` 确认仅修改 new-session.tsx
- commit：未提交（待用户手动验证后确认）
- 已知遗留：点击文件暂无响应（draft 页无 tab 系统）；无切换按钮和 `Ctrl+\` 快捷键
- 详细日志：[20260703开发日志-新建会话页面加载文件树侧边栏.md](../日志/20260703开发日志-新建会话页面加载文件树侧边栏.md)

### fix(app): 修复未登录访问导致页面无限刷新

- 背景：上一提交给 `fetchWithCredentials` 加的 401 拦截逻辑过于激进——未登录用户首次访问页面时,SDK 的 session/config 请求也会收到 401,触发 `window.location.href = "/"` 跳转,页面重载后又收到 401,形成无限刷新死循环。
- 改动文件：
  - `packages/app/src/utils/server.ts`（L20-58：新增模块级 `hasEverSucceeded` 标志,仅在本次页面加载期间 SDK 曾请求成功过(2xx)后才拦截 401。未登录用户首次访问从未成功 → 不拦截,交给 AuthGate 显示登录页;已登录用户 cookie 失效 → 拦截跳转登录页;跳转后页面重载标志重置,不会死循环）
- 影响范围：仅前端 SDK fetch 包装逻辑,不影响后端
- 验证：未登录访问 http://localhost:3000/ 正常显示登录页,不再无限刷新
- 详细日志：[20260703开发日志-取消OPENCODE_SERVER_PASSWORD实现多用户隔离.md](../日志/20260703开发日志-取消OPENCODE_SERVER_PASSWORD实现多用户隔离.md)

### fix(opencode,app): 取消 OPENCODE_SERVER_PASSWORD 实现多用户隔离

- 背景：之前多用户隔离依赖 `OPENCODE_SERVER_PASSWORD` 环境变量，未设置时认证中间件 `authorizationLayer` 提前 return 变成"放行模式"，导致 cookie 验证被跳过、CurrentUser 不注入、session 的 user_id 写成 NULL，隔离失效。设置密码后浏览器会弹原生 Basic Auth 框（第一次密码），进登录页又要输系统用户密码（第二次），体验差且 Basic Auth 用全局共享密码无法区分多用户。
- 改动文件：
  - `packages/opencode/src/server/routes/instance/httpapi/middleware/authorization.ts`（L131-190：去掉 `if (!ServerAuth.required(config)) return` 提前 return，让 cookie 验证始终生效；Basic Auth 部分用 `if (ServerAuth.required(config))` 包裹保留向后兼容；无密码时 401 不带 `www-authenticate` 头避免浏览器弹框）
  - `packages/app/src/utils/server.ts`（L20-58：新增模块级 `isRedirectingToLogin`、`hasEverSucceeded` 标志，`fetchWithCredentials` 用 `Object.assign` 包装保留 `preconnect` 属性，仅在曾成功后收到 401 时清 localStorage 并跳转登录页）
- 影响范围：
  - 后端：API 中间件在无密码时也走 cookie 验证，CLI/SDK 失去 Basic Auth 访问能力（如需 CLI 支持需另设计 token 方案）
  - 前端：cookie 失效时自动跳转登录页，不再静默退化到隔离失效状态
  - UI 静态资源中间件 `authorizationRouterMiddleware` 保持不变，登录页能正常加载
- 验证步骤：
  - 后端 typecheck 无新增错误（预先存在的 effect/drizzle 版本冲突错误与本次改动无关）
  - 前端 typecheck 通过（server.ts 的 preconnect 错误已用 Object.assign 解决）
  - 前端 dev server 编译成功，HTTP 200
  - 无 cookie 访问 `/session` → HTTP 401 ✅
  - 李杰 token 带 cookie 访问 `/session` → HTTP 200 ✅
  - 李杰 token 创建 session → user_id = "usr_李杰" ✅
  - 庞定平 token 创建 session → user_id = "usr_庞定平" ✅
  - 李杰查 session 列表 → 只看到自己的 1 个 session ✅
  - 庞定平查 session 列表 → 只看到自己的 1 个 session ✅
  - 未登录访问首页 → 正常显示登录页,不再无限刷新 ✅
- 已知遗留：
  - CLI/SDK 用户在无密码时无法访问 API（无 cookie 机制）
  - SSE 流式请求中途 cookie 失效不会触发跳转，流会静默断开
- 详细日志：[20260703开发日志-取消OPENCODE_SERVER_PASSWORD实现多用户隔离.md](../日志/20260703开发日志-取消OPENCODE_SERVER_PASSWORD实现多用户隔离.md)

## 2026-07-02

### feat(app): redirect to new session page on cross-user 404
- 背景：完成多用户 session 隔离后，庞定平访问李杰的 session URL 仍会显示空白 404 页面，需要改为跳转到新会话页面
- 改动文件：
  - `packages/app/src/pages/session.tsx`（L33 导入 useNavigate、L205 useNavigate()、L635-678 createResource 的 fetcher 改为 async，await sync.session.sync 后判断并跳转）
  - `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts`（requireSession / list / create 应用 user_id 隔离）
  - `packages/opencode/src/session/session.ts`（SessionInfo schema、CreateInput、ListInput、toRow/fromRow 接入 user_id）
- 关键技术点：
  - `pickSessionCacheEvictions` 会把当前 sessionID 加到 seen set，导致 `tracked` 为 true，404 错误**会被 throw**而不是静默吞掉
  - 解决方法：在 fetcher 中用 `try/catch` 包裹 `await sync.session.sync(id)`，统一吞掉错误后再判断
  - vite 5+ 把源码作为 base64 内联在编译输出中，调试时可以通过请求 3000 端口的 module URL 来确认最新代码
- 验证步骤：
  - 4 个测试用例全部通过：跨用户 404 跳转、李杰自己访问、admin 访问、session 不存在
  - typecheck 无新增错误（message-timeline / settings / server 的错误是预先存在的）
- commit：
  - c9027e4 feat(core): filter sessions by user_id in list and create
  - aa9d50f feat(opencode): enforce session ownership in HTTP API
  - 82337e6 feat(app): redirect to new session page on cross-user 404
- 已知遗留：无
- 详细日志：[20260702开发日志-session-404跳转新会话页面.md](../日志/20260702开发日志-session-404跳转新会话页面.md)


